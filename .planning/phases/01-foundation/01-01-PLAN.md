---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/dev-lifecycle/references/role-matrix.md
  - ~/.claude/skills/dev-lifecycle/references/stage-transitions.md
  - ~/.claude/skills/dev-lifecycle/references/project-detection.md
  - ~/.claude/skills/dev-lifecycle/references/skill-invocation.md
  - ~/.claude/skills/dev-lifecycle/templates/state.json
  - ~/.claude/skills/dev-lifecycle/templates/manifest.json
autonomous: true
requirements: [FOUND-02, FOUND-03, FOUND-05]

must_haves:
  truths:
    - "state.json template contains all fields needed to track current stage, feature, progress, and session info"
    - "manifest.json template contains artifact registry structure with per-stage inputs/outputs and gate rules"
    - "Role matrix document maps all 9 stages to primary/supporting skills with clear dev-lifecycle responsibilities"
    - "Stage transition rules define gate conditions that prevent skipping stages without required artifacts"
    - "Project type detection patterns cover all major ecosystems (Node, Flutter, Rust, Go, Python, iOS, Android, .NET, C/C++)"
  artifacts:
    - path: "~/.claude/skills/dev-lifecycle/references/role-matrix.md"
      provides: "FOUND-05 deliverable: 9-stage skill role mapping"
      contains: "Stage.*Primary Skill.*Supporting"
    - path: "~/.claude/skills/dev-lifecycle/references/stage-transitions.md"
      provides: "Gate logic for stage transitions"
      contains: "gate_rules"
    - path: "~/.claude/skills/dev-lifecycle/references/project-detection.md"
      provides: "Auto-detect project type patterns"
      contains: "package.json"
    - path: "~/.claude/skills/dev-lifecycle/references/skill-invocation.md"
      provides: "How to invoke GSD, PDCA, ADR, retro, work-log skills"
      contains: "/gsd:plan-phase"
    - path: "~/.claude/skills/dev-lifecycle/templates/state.json"
      provides: "FOUND-02 initial state template"
      contains: "current"
    - path: "~/.claude/skills/dev-lifecycle/templates/manifest.json"
      provides: "FOUND-03 initial manifest template"
      contains: "artifacts"
  key_links:
    - from: "templates/state.json"
      to: "references/stage-transitions.md"
      via: "state.json status values must match transition gate expectations"
      pattern: "not_started|in_progress|completed|blocked|skipped"
    - from: "templates/manifest.json"
      to: "references/stage-transitions.md"
      via: "manifest gate_rules reference artifact entries that manifest.json schema defines"
      pattern: "gate_rules"
---

<objective>
Create the foundation artifacts that the SKILL.md rewrite (Plan 02) will reference: references/ files for role matrix, stage transitions, project detection, and skill invocation, plus templates/ for state.json and manifest.json.

Purpose: These are the building blocks of the dev-lifecycle skill. SKILL.md uses progressive disclosure -- it points to references/ for detailed logic. The templates define the runtime state format that all stages will read/write.
Output: 6 files in ~/.claude/skills/dev-lifecycle/ (4 references, 2 templates)
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create references/ files (role-matrix, stage-transitions, project-detection, skill-invocation)</name>
  <files>
    ~/.claude/skills/dev-lifecycle/references/role-matrix.md
    ~/.claude/skills/dev-lifecycle/references/stage-transitions.md
    ~/.claude/skills/dev-lifecycle/references/project-detection.md
    ~/.claude/skills/dev-lifecycle/references/skill-invocation.md
  </files>
  <read_first>
    ~/.claude/skills/dev-lifecycle/SKILL.md
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
Create 4 markdown files in `~/.claude/skills/dev-lifecycle/references/`. Create the `references/` directory first if it does not exist.

**1. role-matrix.md** (FOUND-05 deliverable)

Must contain:
- Title: `# Skill Role Matrix`
- A table with columns: `| Stage | Stage Name | Primary Skill | Supporting Skills | dev-lifecycle Role |`
- All 9 stages mapped:
  - Stage 1 PLAN: Primary=GSD (`/gsd:plan-phase`, `/gsd:discuss-phase`), Supporting=PDCA (`/pdca plan`), ADR (trade-off detection). Role=Trigger pre-flight check, invoke GSD, register plan artifacts
  - Stage 2 DO: Primary=PDCA (`/pdca do`), Supporting=ADR (auto-detect decisions), WHY+SEE comments. Role=Track implementation progress, update state.json
  - Stage 3 TEST: Primary=GSD (`/gsd:verify-work`), Supporting=PDCA (`/pdca analyze`). Role=Run verification, compare against manifest, update status
  - Stage 4 COMMIT: Primary=Git (direct), Supporting=ADR (reference in commit msg). Role=Verify artifacts exist before allowing commit
  - Stage 5 DEPLOY: Primary=Project-specific template, Supporting=None. Role=Provide deploy template, track deploy artifact
  - Stage 6 DEPLOY TEST: Primary=Project-specific template, Supporting=None. Role=Provide smoke test template, gate next stage
  - Stage 7 DOCUMENT: Primary=canvas-design (optional), Supporting=Mermaid (inline). Role=Trigger documentation generation
  - Stage 8 RETROSPECT: Primary=gsd-retrospective, Supporting=ADR (gap check), work-log. Role=Invoke retro skill, update CLAUDE.md
  - Stage 9 PROMOTE: Primary=Manual / optional, Supporting=None. Role=Provide promote checklist
- Key principle section: "dev-lifecycle NEVER executes stage work directly. It: 1. Detects current stage from state.json, 2. Invokes the appropriate primary skill, 3. Collects outputs into manifest.json, 4. Gates transition to the next stage, 5. Updates state.json"
- Section on user freedom: "Users can invoke GSD, PDCA, ADR skills directly at any time. dev-lifecycle orchestrates when overlap occurs but never blocks direct skill usage."

**2. stage-transitions.md**

Must contain:
- Title: `# Stage Transition Rules`
- Section `## Status Values` listing: `not_started`, `in_progress`, `completed`, `blocked`, `skipped`
- Section `## Transition Gates` with a table: `| From | To | Gate Condition |`
  - 1 PLAN -> 2 DO: `manifest.artifacts.1_plan.outputs.length > 0 && manifest.artifacts.1_plan.completed_at != null`
  - 2 DO -> 3 TEST: `manifest.artifacts.2_do.outputs.length > 0`
  - 3 TEST -> 4 COMMIT: `manifest.artifacts.3_test.outputs.length > 0` (Note: strict verification belongs in later phases when E2E spec is implemented)
  - 4 COMMIT -> 5 DEPLOY: `manifest.artifacts.4_commit.outputs.length > 0`
  - 5 DEPLOY -> 6 DEPLOY TEST: `manifest.artifacts.5_deploy.outputs.length > 0`
  - 6 DEPLOY TEST -> 7 DOCUMENT: `manifest.artifacts.6_deploy_test.outputs.length > 0`
  - 7 DOCUMENT -> 8 RETROSPECT: `manifest.artifacts.7_document.outputs.length > 0`
  - 8 RETROSPECT -> 9 PROMOTE: `manifest.artifacts.8_retrospect.outputs.length > 0`
- Section `## Mode-Based Stage Skipping` with table:
  - hotfix: stages 1,7,8,9 skippable
  - feature: stages 5,6,9 skippable
  - release: stage 9 skippable
  - milestone: no stages skippable
- Section `## Transition Procedure` with steps: 1. Read state.json, 2. Check gate condition for current->next, 3. If gate passes: update state.json (stage, stage_name, status=not_started), 4. If gate fails: set status=blocked, show missing artifacts, 5. Write history entry to `.lifecycle/history/`

**3. project-detection.md**

Must contain:
- Title: `# Project Type Detection`
- Section `## Detection Markers` with table: `| Marker File | Project Type |`
  - `package.json` -> `node-web` (check for framework: next, react, vue, angular, express, fastify)
  - `pubspec.yaml` -> `flutter`
  - `Cargo.toml` -> `rust`
  - `go.mod` -> `go`
  - `requirements.txt` or `pyproject.toml` -> `python`
  - `*.xcodeproj` or `*.xcworkspace` -> `ios`
  - `build.gradle` or `build.gradle.kts` -> `android-jvm`
  - `Makefile` (alone) -> `c-cpp`
  - `*.sln` or `*.csproj` -> `dotnet`
- Section `## Detection Flow` with numbered steps: 1. Scan project root, 2. Determine type (may be multi), 3. Present to user for confirmation, 4. Store in state.json project.detected_type and project.confirmed, 5. Use type for stage-specific templates
- Section `## Multi-Type Projects` explaining that projects can be multi-type (e.g., Flutter frontend + Python backend) and how to handle that (store as array in detected_type, user confirms)

**4. skill-invocation.md**

Must contain:
- Title: `# Skill Invocation Guide`
- For each skill, a section with: skill name, available commands, when dev-lifecycle invokes it, expected output
  - **GSD**: `/gsd:discuss-phase`, `/gsd:plan-phase`, `/gsd:execute-phase`, `/gsd:verify-work`, `/gsd:complete-milestone`. Invoked at: Stage 1 (plan), Stage 3 (verify)
  - **PDCA**: `/pdca plan`, `/pdca do`, `/pdca check`, `/pdca analyze`. Invoked at: Stage 1 (supporting plan), Stage 2 (primary do), Stage 3 (supporting analyze)
  - **ADR**: `/adr`. Invoked at: Stage 1 (trade-off detection), Stage 2 (auto-detect decisions), Stage 4 (reference in commit), Stage 8 (gap check)
  - **gsd-retrospective**: `/gsd-retrospective`. Invoked at: Stage 8
  - **work-log**: `/work-log`. Invoked at: Stage 8 (Obsidian vault logging)
  - **canvas-design**: `/canvas-design`. Invoked at: Stage 7 (optional, architecture diagrams)
- Section `## Invocation Pattern`: "dev-lifecycle suggests the skill invocation command to the user. It does NOT auto-execute skills. The user triggers the skill, and dev-lifecycle tracks the output artifact."
  </action>
  <verify>
    <automated>test -f ~/.claude/skills/dev-lifecycle/references/role-matrix.md && test -f ~/.claude/skills/dev-lifecycle/references/stage-transitions.md && test -f ~/.claude/skills/dev-lifecycle/references/project-detection.md && test -f ~/.claude/skills/dev-lifecycle/references/skill-invocation.md && echo "ALL REFERENCES EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - role-matrix.md contains "| Stage | Stage Name | Primary Skill | Supporting Skills | dev-lifecycle Role |"
    - role-matrix.md contains all 9 stage rows (grep for "PLAN", "DO", "TEST", "COMMIT", "DEPLOY", "DEPLOY TEST", "DOCUMENT", "RETROSPECT", "PROMOTE")
    - role-matrix.md contains "dev-lifecycle NEVER executes stage work directly"
    - stage-transitions.md contains "not_started" and "in_progress" and "completed" and "blocked" and "skipped"
    - stage-transitions.md contains "Gate Condition" table with 8 transition rules
    - stage-transitions.md contains "hotfix" and "feature" and "release" and "milestone" mode-based skipping
    - project-detection.md contains "package.json" and "pubspec.yaml" and "Cargo.toml" and "go.mod" and "pyproject.toml"
    - skill-invocation.md contains "/gsd:plan-phase" and "/pdca do" and "/adr" and "/gsd-retrospective" and "/work-log"
    - No file contains "muse" or "opc@" or "flutter build apk" or "PIN" (muse-specific references)
  </acceptance_criteria>
  <done>All 4 references/ files exist with complete content. Role matrix covers all 9 stages. Stage transitions define all 8 gate rules and 4 execution modes. Project detection covers 9+ project types. Skill invocation documents all 6 skills with commands and invocation patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Create templates/ files (state.json, manifest.json)</name>
  <files>
    ~/.claude/skills/dev-lifecycle/templates/state.json
    ~/.claude/skills/dev-lifecycle/templates/manifest.json
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    ~/.claude/skills/dev-lifecycle/references/stage-transitions.md
  </read_first>
  <action>
Create `~/.claude/skills/dev-lifecycle/templates/` directory and two JSON template files.

**1. state.json** (FOUND-02: session-persistent state tracking)

Exact content:
```json
{
  "version": "1.0",
  "project": {
    "name": "",
    "type": "auto",
    "detected_type": null,
    "confirmed": false
  },
  "current": {
    "stage": 1,
    "stage_name": "PLAN",
    "status": "not_started",
    "feature": null,
    "mode": "feature"
  },
  "progress": {
    "stages_completed": [],
    "current_stage_started_at": null,
    "last_transition_at": null
  },
  "session": {
    "last_active": null,
    "resume_hint": ""
  }
}
```

Fields explanation (include as comments in a companion README or inline -- since JSON does not support comments, add a `_schema` top-level key):
- `version`: Schema version for future migration
- `project.name`: Detected or user-provided project name
- `project.type`: "auto" until detection runs
- `project.detected_type`: Result of project-detection.md logic (string or array for multi-type)
- `project.confirmed`: true after user confirms detected type
- `current.stage`: Integer 1-9
- `current.stage_name`: Human-readable stage name (PLAN, DO, TEST, COMMIT, DEPLOY, DEPLOY TEST, DOCUMENT, RETROSPECT, PROMOTE)
- `current.status`: One of not_started, in_progress, completed, blocked, skipped
- `current.feature`: Active feature name (null if no feature selected)
- `current.mode`: One of feature, hotfix, release, milestone
- `progress.stages_completed`: Array of completed stage numbers
- `session.last_active`: ISO 8601 timestamp
- `session.resume_hint`: Human-readable hint for session resumption

Do NOT add a `_schema` key -- keep the JSON clean. The schema documentation lives in the RESEARCH.md already.

**2. manifest.json** (FOUND-03: artifact registry connecting stage outputs to inputs)

Exact content:
```json
{
  "version": "1.0",
  "feature": "",
  "artifacts": {
    "1_plan": {
      "required_inputs": [],
      "outputs": [],
      "completed_at": null
    },
    "2_do": {
      "required_inputs": ["1_plan"],
      "outputs": [],
      "completed_at": null
    },
    "3_test": {
      "required_inputs": ["2_do"],
      "outputs": [],
      "completed_at": null
    },
    "4_commit": {
      "required_inputs": ["3_test"],
      "outputs": [],
      "completed_at": null
    },
    "5_deploy": {
      "required_inputs": ["4_commit"],
      "outputs": [],
      "completed_at": null
    },
    "6_deploy_test": {
      "required_inputs": ["5_deploy"],
      "outputs": [],
      "completed_at": null
    },
    "7_document": {
      "required_inputs": ["6_deploy_test"],
      "outputs": [],
      "completed_at": null
    },
    "8_retrospect": {
      "required_inputs": ["7_document"],
      "outputs": [],
      "completed_at": null
    },
    "9_promote": {
      "required_inputs": ["8_retrospect"],
      "outputs": [],
      "completed_at": null
    }
  },
  "gate_rules": {
    "1_to_2": "artifacts.1_plan.outputs.length > 0",
    "2_to_3": "artifacts.2_do.outputs.length > 0",
    "3_to_4": "artifacts.3_test.outputs.length > 0",
    "4_to_5": "artifacts.4_commit.outputs.length > 0",
    "5_to_6": "artifacts.5_deploy.outputs.length > 0",
    "6_to_7": "artifacts.6_deploy_test.outputs.length > 0",
    "7_to_8": "artifacts.7_document.outputs.length > 0",
    "8_to_9": "artifacts.8_retrospect.outputs.length > 0"
  }
}
```

Each artifact output entry (when populated during usage) will have this shape:
```json
{
  "type": "plan|spec|prototype|code|test|commit|deploy|report|retro|demo",
  "path": "relative/path/to/artifact",
  "created_at": "ISO-8601",
  "status": "complete|partial|failed"
}
```

This shape is documented in stage-transitions.md and RESEARCH.md, not in the template itself.
  </action>
  <verify>
    <automated>python3 -c "import json; s=json.load(open('$HOME/.claude/skills/dev-lifecycle/templates/state.json')); assert 'version' in s and 'current' in s and 'progress' in s and 'session' in s and s['current']['stage'] == 1; print('state.json VALID')" && python3 -c "import json; m=json.load(open('$HOME/.claude/skills/dev-lifecycle/templates/manifest.json')); assert 'artifacts' in m and '1_plan' in m['artifacts'] and '9_promote' in m['artifacts'] and 'gate_rules' in m; print('manifest.json VALID')"</automated>
  </verify>
  <acceptance_criteria>
    - templates/state.json is valid JSON (python3 json.load succeeds)
    - state.json contains keys: "version", "project", "current", "progress", "session"
    - state.json current.stage == 1, current.stage_name == "PLAN", current.status == "not_started"
    - state.json current.mode == "feature"
    - state.json project.type == "auto"
    - templates/manifest.json is valid JSON
    - manifest.json contains "artifacts" key with all 9 stages: "1_plan" through "9_promote"
    - manifest.json contains "gate_rules" key with 8 transition rules: "1_to_2" through "8_to_9"
    - manifest.json "2_do" has required_inputs containing "1_plan"
    - No file contains "muse" or "opc@" references
  </acceptance_criteria>
  <done>Both JSON templates exist, are valid JSON, and contain the complete schema. state.json tracks project info, current stage/status/feature/mode, progress, and session info. manifest.json tracks per-stage artifacts with inputs/outputs and gate rules for all 8 stage transitions.</done>
</task>

</tasks>

<verification>
All 6 files exist in ~/.claude/skills/dev-lifecycle/:
- references/role-matrix.md
- references/stage-transitions.md
- references/project-detection.md
- references/skill-invocation.md
- templates/state.json
- templates/manifest.json

No file contains muse-specific references:
```bash
grep -ri "muse\|opc@\|muse-backend\|flutter build apk\|PIN" ~/.claude/skills/dev-lifecycle/references/ ~/.claude/skills/dev-lifecycle/templates/ 2>/dev/null | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- FOUND-02: state.json template exists with stage, status, feature, mode, progress, session fields
- FOUND-03: manifest.json template exists with 9-stage artifact registry and 8 gate rules
- FOUND-05: role-matrix.md exists mapping all 9 stages to primary/supporting skills
- All supporting references (stage-transitions, project-detection, skill-invocation) exist
- Zero muse-specific hardcoding in any created file
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
