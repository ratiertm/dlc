---
phase: 07-commit-stage
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - skill/SKILL.md
  - skill/references/role-matrix.md
  - ~/.claude/skills/dev-lifecycle/SKILL.md
  - ~/.claude/skills/dev-lifecycle/references/role-matrix.md
autonomous: true
requirements: [PIPE-04, ARCH-04]

must_haves:
  truths:
    - "SKILL.md Stage 4 section has dev-lifecycle as primary with Read directive to commit-stage.md"
    - "role-matrix.md Stage 4 reflects dev-lifecycle orchestration with Git as direct tool"
    - "Runtime skill directory matches version-controlled source"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Updated Stage 4 section with Read directive and pipeline steps"
      contains: "commit-stage.md"
    - path: "skill/references/role-matrix.md"
      provides: "Updated Stage 4 row with dev-lifecycle orchestration"
      contains: "COMMIT"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/commit-stage.md"
      via: "Read directive in Stage 4 section"
      pattern: "Read.*commit-stage\\.md"
---

<objective>
Update SKILL.md Stage 4 section and role-matrix.md to reflect dev-lifecycle as the COMMIT Stage orchestrator with a Read directive to commit-stage.md.

Purpose: Completes the inner loop skill alignment -- all 4 stages (PLAN, DO, TEST, COMMIT) will have dev-lifecycle as primary with Read directives to their respective adapter references.

Output: Updated SKILL.md and role-matrix.md (version-controlled + runtime copies)
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-commit-stage/07-CONTEXT.md
@.planning/phases/06-test-stage/06-02-SUMMARY.md

<interfaces>
<!-- Executor MUST read these to understand the update pattern established in Phases 4-6 -->

From skill/SKILL.md Stage 3 section (pattern to follow for Stage 4 update):
```markdown
### Stage 3: TEST

Purpose: Verify implementation against E2E spec (per-step pass/fail)...
Primary: dev-lifecycle (spec + prototype verification)
Supporting: GSD (`/gsd:verify-work`), PDCA (`/pdca analyze`)
Outputs: Verification report (verification.json + verification.md), user behavioral checklist

Read: $CLAUDE_SKILL_DIR/references/test-stage.md

Pipeline:
1. Stage initialization (gate check 2_to_3, load spec + prototype)
2. Spec step enumeration (parse all steps, display verification plan)
...
```

From skill/references/role-matrix.md Stage 4 row (current):
```
| 4 | COMMIT | Git (direct) | ADR (reference in commit msg) | Verify artifacts exist before allowing commit |
```

Current SKILL.md Stage 4 section (lines 104-116):
```markdown
### Stage 4: COMMIT

Purpose: Save verified work with meaningful commit history.
Tool: Git (direct)
Supporting: ADR (reference in commit message)
Outputs: Commit hash, version tag (optional)

Rules:
- Commit message focuses on "why", not "what"
- Version tag format: `v{major}.{minor}.{patch}`
- Exclude sensitive files (`.env`, credentials)
- Reference related ADRs: `(ADR-NNN)` in commit message
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md Stage 4 and role-matrix.md Stage 4</name>
  <files>skill/SKILL.md, skill/references/role-matrix.md</files>
  <read_first>
    - skill/SKILL.md (full file -- find Stage 4 section to replace, reference Stage 3 section format)
    - skill/references/role-matrix.md (full file -- find Stage 4 row in all tables)
    - skill/references/commit-stage.md (just created in plan 07-01 -- know the step names)
    - .planning/phases/06-test-stage/06-02-SUMMARY.md (pattern for this type of update)
  </read_first>
  <action>
**SKILL.md update:**

Replace the current Stage 4 section (lines 104-116, from "### Stage 4: COMMIT" through the Rules list, stopping before "### Stage 5: DEPLOY") with a new section matching the Stage 3 format:

```markdown
### Stage 4: COMMIT

Purpose: Verify all spec steps passed, validate required artifacts exist, generate why-centric commit message with ADR references. Blocks on verification failures with specific gap list. Dual gate: both Claude structural and user behavioral verification must have passed in TEST stage.
Primary: dev-lifecycle (verification gate + commit orchestration)
Supporting: Git (direct commit execution), ADR (reference in commit msg)
Outputs: Commit hash, version tag (optional)

Read: $CLAUDE_SKILL_DIR/references/commit-stage.md

Pipeline:
1. Stage initialization (gate check 3_to_4, content-level verification.json check)
2. Verification gate display (gap list if blocked, override option)
3. Commit preparation (collect files, detect ADRs, generate why-centric message)
4. Git commit execution (stage files, exclude sensitive/runtime, capture hash)
5. Completion (register outputs in manifest, update state, announce)
```

Do NOT change any other section of SKILL.md. Only replace the Stage 4 block.

**role-matrix.md update:**

In the Stage-Skill Mapping table, update Stage 4 row:
```
| 4 | COMMIT | dev-lifecycle (verification gate + commit orchestration) | Git (direct), ADR (reference in commit msg) | Enforce verification gate, generate why-centric commit, register outputs |
```

In the Skill Overlap Resolution table, add a new row for Stage 4:
```
| dev-lifecycle vs Git in Stage 4 | dev-lifecycle is primary for COMMIT orchestration (verification gate, message generation, artifact validation). Git is the direct tool for commit execution. These are complementary. |
```

In the Stage Output Types table, update Stage 4 row:
```
| 4 COMMIT | commit-hash (required), tag (optional) |
```
(Add "(required)" to commit-hash matching the pattern from Stages 1-3)

Do NOT change any other rows or sections.
  </action>
  <verify>
    <automated>grep "Read.*commit-stage.md" skill/SKILL.md && grep "dev-lifecycle.*verification gate" skill/references/role-matrix.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Read.*commit-stage.md" skill/SKILL.md returns match (Read directive present)
    - grep "Pipeline:" skill/SKILL.md returns match in Stage 4 section
    - grep "dev-lifecycle.*verification gate" skill/SKILL.md returns match (primary skill)
    - grep "dev-lifecycle.*verification gate\|dev-lifecycle.*commit orchestration" skill/references/role-matrix.md returns match
    - grep "commit-hash (required)" skill/references/role-matrix.md returns match
    - Stage 5 section in SKILL.md is unchanged (grep "### Stage 5: DEPLOY" still present)
  </acceptance_criteria>
  <done>SKILL.md Stage 4 section updated with dev-lifecycle primary, Read directive, and 5-step pipeline. role-matrix.md Stage 4 updated across all tables.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy updated SKILL.md and role-matrix.md to runtime</name>
  <files>~/.claude/skills/dev-lifecycle/SKILL.md, ~/.claude/skills/dev-lifecycle/references/role-matrix.md</files>
  <read_first>
    - skill/SKILL.md (source of truth after Task 1 update)
    - skill/references/role-matrix.md (source of truth after Task 1 update)
  </read_first>
  <action>
Copy the updated files to the runtime skill directory:

1. `cp skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md`
2. `cp skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md`

Verify both copies match their sources with `diff`.

This is the same deployment pattern used in 04-02, 05-02, and 06-02.
  </action>
  <verify>
    <automated>diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md && diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md && echo "MATCH"</automated>
  </verify>
  <acceptance_criteria>
    - diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md shows no differences
    - diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md shows no differences
  </acceptance_criteria>
  <done>Runtime copies at ~/.claude/skills/dev-lifecycle/ match version-controlled sources for both SKILL.md and role-matrix.md</done>
</task>

</tasks>

<verification>
1. `grep "Read.*commit-stage.md" skill/SKILL.md` -- Read directive present
2. `grep "Pipeline:" skill/SKILL.md` -- pipeline steps listed in Stage 4
3. `grep "dev-lifecycle" skill/references/role-matrix.md | grep -i "commit"` -- role-matrix updated
4. `diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md` -- runtime matches
5. `diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md` -- runtime matches
</verification>

<success_criteria>
- SKILL.md Stage 4 section matches the format of Stages 1-3 (Primary, Supporting, Outputs, Read directive, Pipeline)
- role-matrix.md Stage 4 updated in all relevant tables
- All 4 inner loop stages now have dev-lifecycle as primary with Read directives
- Runtime copies verified identical to version-controlled sources
</success_criteria>

<output>
After completion, create `.planning/phases/07-commit-stage/07-02-SUMMARY.md`
</output>
