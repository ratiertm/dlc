---
phase: 04-plan-stage
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - skill/SKILL.md
  - skill/references/role-matrix.md
  - skill/references/skill-invocation.md
autonomous: true
requirements: [PIPE-01]

must_haves:
  truths:
    - "SKILL.md Stage 1 section points to plan-stage.md and describes dev-lifecycle as primary for spec+prototype generation"
    - "role-matrix.md reflects dev-lifecycle as primary skill for Stage 1 PLAN (spec+prototype), GSD as supporting"
    - "skill-invocation.md Stage 1 section describes the independent spec+prototype generation flow"
    - "Stage 1 outputs in role-matrix include spec (required) and prototype (required), not optional"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Updated Stage 1 PLAN section with plan-stage.md reference"
      contains: "plan-stage.md"
    - path: "skill/references/role-matrix.md"
      provides: "Updated Stage 1 primary skill to dev-lifecycle"
      contains: "dev-lifecycle"
    - path: "skill/references/skill-invocation.md"
      provides: "Updated Stage 1 invocation description"
      contains: "plan-stage.md"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/plan-stage.md"
      via: "Read directive in Stage 1 section"
      pattern: "plan-stage\\.md"
    - from: "skill/references/role-matrix.md"
      to: "skill/references/plan-stage.md"
      via: "Stage 1 primary skill reference"
      pattern: "plan-stage\\.md"
---

<objective>
Update existing skill documentation to reflect the Phase 4 decision: dev-lifecycle independently generates spec+prototype in Stage 1 PLAN, with GSD as supporting (not primary).

Purpose: SKILL.md, role-matrix.md, and skill-invocation.md currently describe GSD as primary for Stage 1. Per CONTEXT.md locked decision, dev-lifecycle is primary for spec+prototype generation. These files must be updated to avoid confusion when Claude reads them during Stage 1 execution.

Output: Updated SKILL.md, role-matrix.md, skill-invocation.md -- all aligned with plan-stage.md.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plan-stage/04-CONTEXT.md
@.planning/phases/04-plan-stage/04-RESEARCH.md
@.planning/phases/04-plan-stage/04-01-SUMMARY.md

<interfaces>
<!-- Current state of files to be modified -->

From skill/SKILL.md (lines 55-66, Stage 1 section):
```
### Stage 1: PLAN

Purpose: Define scope, review lessons, create execution plan.
Primary skill: GSD (`/gsd:plan-phase`, `/gsd:discuss-phase`)
Supporting: PDCA (`/pdca plan`), ADR (trade-off detection)
Outputs: PLAN.md, pre-flight check, PDCA plan entry

Pre-flight checks:
- Read previous retrospective "Lessons for Next Phase"
- Surface related ADRs for the affected domain
- Check PDCA status for unresolved gaps
- Scan for technical debt (TODO/FIXME/HACK)
```

From skill/references/role-matrix.md (line 9, Stage 1 row):
```
| 1 | PLAN | GSD (`/gsd:plan-phase`, `/gsd:discuss-phase`) | PDCA (`/pdca plan`), ADR (trade-off detection) | Trigger pre-flight check, invoke GSD, register plan artifacts |
```

From skill/references/role-matrix.md (lines 51, Stage 1 output):
```
| 1 PLAN | plan, spec (optional) |
```

From skill/references/skill-invocation.md (lines 14-19, Stage 1 section):
```
**When dev-lifecycle invokes:**
- **Stage 1 PLAN:** `/gsd:discuss-phase` then `/gsd:plan-phase` to create structured plans

**Expected output:**
- Stage 1: CONTEXT.md, RESEARCH.md, PLAN.md files in `.planning/phases/`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md Stage 1 section</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md
    - .planning/phases/04-plan-stage/04-CONTEXT.md
  </read_first>
  <action>
Update ONLY the Stage 1 PLAN section (lines 55-66) of skill/SKILL.md. Keep everything else unchanged.

Replace the current Stage 1 section with:

```
### Stage 1: PLAN

Purpose: Define scope, review lessons, generate E2E spec and clickable prototype, get user agreement.
Primary: dev-lifecycle (spec + prototype generation)
Supporting: GSD (broader project planning if needed), PDCA (`/pdca plan`), ADR (trade-off detection)
Outputs: E2E spec (.spec.md), clickable prototype (.prototype.html), pre-flight check

Read: `$CLAUDE_SKILL_DIR/references/plan-stage.md`

Pipeline:
1. Preflight check (retrospective lessons first, non-blocking warnings)
2. E2E Spec generation (fill spec-template.md, all 5 chain steps)
3. Prototype generation (from spec, dual spec linking)
4. Agreement gate (user clicks prototype in browser, hard block until approval)
```

This adds a Read directive to plan-stage.md and changes primary from GSD to dev-lifecycle per CONTEXT.md decision. The Read directive is critical -- it tells Claude to load the detailed instructions from plan-stage.md.

Also deploy to runtime: copy the updated file to `~/.claude/skills/dev-lifecycle/SKILL.md`
  </action>
  <verify>
    <automated>grep -A 12 "### Stage 1: PLAN" skill/SKILL.md | grep -c "plan-stage.md\|dev-lifecycle\|Agreement gate" | awk '{if ($1 >= 3) print "OK"; else print "FAIL"}'</automated>
  </verify>
  <acceptance_criteria>
    - grep "plan-stage.md" skill/SKILL.md returns match
    - grep "Primary.*dev-lifecycle" skill/SKILL.md returns match (or "Primary: dev-lifecycle")
    - grep "Agreement gate" skill/SKILL.md returns match
    - grep "spec.*prototype" skill/SKILL.md returns match in Stage 1 section
    - SKILL.md total line count is still within 260 lines (no bloat)
    - test -f ~/.claude/skills/dev-lifecycle/SKILL.md (runtime deployed)
  </acceptance_criteria>
  <done>SKILL.md Stage 1 section updated to reference plan-stage.md, show dev-lifecycle as primary, and describe the 4-step pipeline. Deployed to runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Update role-matrix.md and skill-invocation.md</name>
  <files>skill/references/role-matrix.md, skill/references/skill-invocation.md</files>
  <read_first>
    - skill/references/role-matrix.md
    - skill/references/skill-invocation.md
    - .planning/phases/04-plan-stage/04-CONTEXT.md
  </read_first>
  <action>
**role-matrix.md changes:**

1. Update Stage 1 row in the Stage-Skill Mapping table (line 9):
   FROM: `| 1 | PLAN | GSD (/gsd:plan-phase, /gsd:discuss-phase) | PDCA (/pdca plan), ADR (trade-off detection) | Trigger pre-flight check, invoke GSD, register plan artifacts |`
   TO: `| 1 | PLAN | dev-lifecycle (spec + prototype generation) | GSD (broader planning), PDCA (/pdca plan), ADR (trade-off detection) | Run preflight, generate spec + prototype, enforce agreement gate |`

2. Update Stage 1 output types (line 51):
   FROM: `| 1 PLAN | plan, spec (optional) |`
   TO: `| 1 PLAN | spec (required), prototype (required) |`

3. Update the Skill Overlap Resolution table -- add or update the row for GSD vs dev-lifecycle:
   FROM: `| GSD /gsd:plan-phase vs PDCA /pdca plan | GSD is primary for Stage 1. PDCA supplements with cycle tracking. |`
   TO: `| dev-lifecycle vs GSD in Stage 1 | dev-lifecycle is primary for spec + prototype generation. GSD is available for broader project planning (roadmap, phases). These are complementary, not competing. |`

**skill-invocation.md changes:**

1. Update the GSD "When dev-lifecycle invokes" section (lines 14-19):
   Change Stage 1 description from "invoke GSD for structured plans" to reflect that dev-lifecycle generates spec+prototype independently:
   FROM: `- **Stage 1 PLAN:** /gsd:discuss-phase then /gsd:plan-phase to create structured plans`
   TO: `- **Stage 1 PLAN:** dev-lifecycle generates spec + prototype independently (Read: $CLAUDE_SKILL_DIR/references/plan-stage.md). GSD available for broader project planning if user invokes directly.`

2. Update GSD Expected output for Stage 1:
   FROM: `- Stage 1: CONTEXT.md, RESEARCH.md, PLAN.md files in .planning/phases/`
   TO: `- Stage 1: .lifecycle/features/{name}/spec.md + prototype.html (via plan-stage.md adapter)`

Also deploy both files to runtime:
- `~/.claude/skills/dev-lifecycle/references/role-matrix.md`
- `~/.claude/skills/dev-lifecycle/references/skill-invocation.md`
  </action>
  <verify>
    <automated>grep "dev-lifecycle" skill/references/role-matrix.md | head -3 && grep "spec.*required.*prototype.*required" skill/references/role-matrix.md && grep "plan-stage.md" skill/references/skill-invocation.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "dev-lifecycle" skill/references/role-matrix.md returns match in Stage 1 row
    - grep "spec (required), prototype (required)" skill/references/role-matrix.md returns match
    - grep "complementary" skill/references/role-matrix.md returns match (overlap resolution)
    - grep "plan-stage.md" skill/references/skill-invocation.md returns match
    - grep "independently" skill/references/skill-invocation.md returns match
    - test -f ~/.claude/skills/dev-lifecycle/references/role-matrix.md (runtime deployed)
    - test -f ~/.claude/skills/dev-lifecycle/references/skill-invocation.md (runtime deployed)
  </acceptance_criteria>
  <done>role-matrix.md updated with dev-lifecycle as primary for Stage 1 and outputs as required (not optional). skill-invocation.md updated to describe independent spec+prototype generation. Both deployed to runtime.</done>
</task>

</tasks>

<verification>
- `grep "plan-stage.md" skill/SKILL.md skill/references/role-matrix.md skill/references/skill-invocation.md | wc -l` -- all 3 files reference plan-stage.md (>= 3, though role-matrix may reference indirectly)
- `grep "dev-lifecycle" skill/references/role-matrix.md` -- Stage 1 primary skill updated
- `grep "required" skill/references/role-matrix.md` -- spec and prototype are required outputs
- `grep "independently" skill/references/skill-invocation.md` -- independent generation described
</verification>

<success_criteria>
- SKILL.md Stage 1 section references plan-stage.md and describes dev-lifecycle as primary
- role-matrix.md Stage 1 row shows dev-lifecycle as primary, GSD as supporting
- role-matrix.md Stage 1 outputs show spec and prototype as required (not optional)
- skill-invocation.md describes independent spec+prototype generation for Stage 1
- All 3 files deployed to ~/.claude/skills/dev-lifecycle/ runtime directory
- No other sections of the files are modified (surgery, not rewrite)
</success_criteria>

<output>
After completion, create `.planning/phases/04-plan-stage/04-02-SUMMARY.md`
</output>
