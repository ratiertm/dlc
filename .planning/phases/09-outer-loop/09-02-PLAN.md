---
phase: 09-outer-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - skill/SKILL.md
  - skill/references/role-matrix.md
autonomous: true
requirements:
  - PIPE-05
  - PIPE-06
  - PIPE-07
  - PIPE-08
  - PIPE-09

must_haves:
  truths:
    - "SKILL.md Stage 5-9 sections have Read directives pointing to new adapter files"
    - "SKILL.md Stage 5-9 sections show dev-lifecycle as primary skill with Pipeline steps"
    - "role-matrix.md Stage 5-9 rows show dev-lifecycle as primary orchestrator"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Stage 5-9 Read directives and pipeline summaries"
      contains: "references/deploy-stage.md"
    - path: "skill/references/role-matrix.md"
      provides: "Updated Stage 5-9 skill mappings"
      contains: "dev-lifecycle"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/deploy-stage.md"
      via: "Read directive"
      pattern: "Read.*deploy-stage.md"
    - from: "skill/SKILL.md"
      to: "skill/references/retrospect-stage.md"
      via: "Read directive"
      pattern: "Read.*retrospect-stage.md"
    - from: "skill/SKILL.md"
      to: "skill/references/promote-stage.md"
      via: "Read directive"
      pattern: "Read.*promote-stage.md"
---

<objective>
Update SKILL.md and role-matrix.md to reflect dev-lifecycle as primary orchestrator for all 9 stages, with Read directives pointing to the new outer loop adapter files.

Purpose: Complete the SKILL.md so Stage 5-9 sections match the pattern of Stage 1-4 (purpose, primary, supporting, outputs, Read directive, Pipeline steps).
Output: Updated SKILL.md and role-matrix.md with consistent stage descriptions.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-outer-loop/09-CONTEXT.md
@.planning/phases/09-outer-loop/09-01-SUMMARY.md

<interfaces>
<!-- SKILL.md Stage 1-4 pattern (each stage section follows this format): -->

```markdown
### Stage {N}: {NAME}

Purpose: {one-line purpose}
Primary: dev-lifecycle ({role description})
Supporting: {skills list}
Outputs: {artifact types}

Read: `$CLAUDE_SKILL_DIR/references/{stage}-stage.md`

Pipeline:
1. {step description}
2. {step description}
...
```

<!-- Current role-matrix.md Stage 5-9 entries to be replaced: -->

| 5 | DEPLOY | Project-specific template | None | Provide deploy template, track deploy artifact |
| 6 | DEPLOY TEST | Project-specific template | None | Provide smoke test template, gate next stage |
| 7 | DOCUMENT | canvas-design (optional) | Mermaid (inline) | Trigger documentation generation |
| 8 | RETROSPECT | gsd-retrospective | ADR (gap check), work-log | Invoke retro skill, update CLAUDE.md |
| 9 | PROMOTE | Manual / optional | None | Provide promote checklist |

<!-- Should become (per RESEARCH.md): -->

| 5 | DEPLOY | dev-lifecycle (deploy orchestration) | Project-specific tools | Provide type-specific checklist, track deploy |
| 6 | DEPLOY TEST | dev-lifecycle (smoke test orchestration) | Project-specific tools | 3-category smoke test framework |
| 7 | DOCUMENT | dev-lifecycle (documentation orchestration) | Mermaid (inline), canvas-design (optional) | Generate diagrams, update docs |
| 8 | RETROSPECT | dev-lifecycle (retrospective orchestration) | gsd-retrospective, ADR (gap check), work-log | Orchestrate retro + ADR gap + work-log + Living State |
| 9 | PROMOTE | dev-lifecycle (optional promotion) | None | Skip-or-proceed flow, demo guidance |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md Stage 5-9 sections with Read directives and pipeline summaries</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (full file -- need to edit Stage 5-9 sections, preserve everything else)
    - skill/references/deploy-stage.md (read Step headings for pipeline summary)
    - skill/references/deploy-test-stage.md (read Step headings for pipeline summary)
    - skill/references/document-stage.md (read Step headings for pipeline summary)
    - skill/references/retrospect-stage.md (read Step headings for pipeline summary)
    - skill/references/promote-stage.md (read Step headings for pipeline summary)
  </read_first>
  <action>
Replace SKILL.md Stage 5-9 sections (lines ~124-180, from "### Stage 5: DEPLOY" through "### Stage 9: PROMOTE" ending before "## State Management") with the Inner Loop pattern format. Each section must have: Purpose, Primary (dev-lifecycle), Supporting, Outputs, Read directive, Pipeline steps.

Specific replacements:

**Stage 5: DEPLOY** -- replace current 5-line placeholder with:
- Purpose: Ship to target environment using project-type-specific checklist. User executes deploy steps; Claude tracks progress.
- Primary: dev-lifecycle (deploy orchestration)
- Supporting: Project-specific tools
- Outputs: Deploy log, deploy artifact
- Read: `$CLAUDE_SKILL_DIR/references/deploy-stage.md`
- Pipeline: 1. Stage init (gate 4_to_5, mode skip check), 2. Project type resolution, 3. Deploy checklist (type-specific), 4. Deploy execution (user-guided), 5. Completion

**Stage 6: DEPLOY TEST** -- replace:
- Purpose: Verify deployment with 3-category smoke tests: health, core flow, resources.
- Primary: dev-lifecycle (smoke test orchestration)
- Supporting: Project-specific tools
- Outputs: Smoke test report
- Read: `$CLAUDE_SKILL_DIR/references/deploy-test-stage.md`
- Pipeline: 1. Stage init (gate 5_to_6, mode skip check), 2. Smoke test plan, 3. Health check, 4. Core flow verification, 5. Resource check, 6. Completion

**Stage 7: DOCUMENT** -- replace:
- Purpose: Record project state with Mermaid architecture/sequence diagrams and update README/CLAUDE.md.
- Primary: dev-lifecycle (documentation orchestration)
- Supporting: Mermaid (inline), canvas-design (optional)
- Outputs: Architecture doc, sequence diagrams
- Read: `$CLAUDE_SKILL_DIR/references/document-stage.md`
- Pipeline: 1. Stage init (gate 6_to_7, mode skip check), 2. Architecture diagram (Mermaid), 3. Sequence diagrams (Mermaid), 4. README/CLAUDE.md update, 5. Completion

**Stage 8: RETROSPECT** -- replace:
- Purpose: Reflect on the feature cycle. Delegate to gsd-retrospective, check ADR gaps, record work log, update Living State.
- Primary: dev-lifecycle (retrospective orchestration)
- Supporting: gsd-retrospective, ADR (gap check), work-log
- Outputs: Retrospective document, missing ADRs (optional)
- Read: `$CLAUDE_SKILL_DIR/references/retrospect-stage.md`
- Pipeline: 1. Stage init (gate 7_to_8, mode skip check), 2. Retrospective generation (delegate to gsd-retrospective), 3. ADR gap check, 4. Work log (delegate to work-log), 5. Living State update, 6. Completion

**Stage 9: PROMOTE** -- replace:
- Purpose: Optionally share the work via demo, announcement, or screenshots. Skip without penalty.
- Primary: dev-lifecycle (optional promotion)
- Supporting: None
- Outputs: Demo, announcement (optional)
- Read: `$CLAUDE_SKILL_DIR/references/promote-stage.md`
- Pipeline: 1. Stage init (gate 8_to_9), 2. Skip check (default: skip), 3. Demo type selection, 4. Demo creation guidance, 5. Completion

IMPORTANT: Preserve all content before Stage 5 section and after Stage 9 section (State Management onward). Only replace the 5 stage sections.
  </action>
  <verify>
    <automated>grep -c 'Read:.*references/.*-stage.md' skill/SKILL.md | xargs -I{} test {} -ge 9 && grep -q "deploy-stage.md" skill/SKILL.md && grep -q "retrospect-stage.md" skill/SKILL.md && grep -q "promote-stage.md" skill/SKILL.md && grep -q "dev-lifecycle (deploy orchestration)" skill/SKILL.md && grep -q "dev-lifecycle (retrospective orchestration)" skill/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md Stage 5-9 sections match the pattern of Stage 1-4 with Purpose, Primary (dev-lifecycle), Supporting, Outputs, Read directive, and Pipeline steps. All 9 stages now have Read directives to adapter reference files.</done>
</task>

<task type="auto">
  <name>Task 2: Update role-matrix.md Stage 5-9 skill mappings</name>
  <files>skill/references/role-matrix.md</files>
  <read_first>
    - skill/references/role-matrix.md (full file -- need to edit Stage 5-9 rows and Skill Overlap Resolution)
  </read_first>
  <action>
Update role-matrix.md with two changes:

1. **Stage-Skill Mapping table** -- replace rows 5-9 with:

| 5 | DEPLOY | dev-lifecycle (deploy orchestration) | Project-specific tools | Provide type-specific deploy checklist, track deploy progress, register artifacts |
| 6 | DEPLOY TEST | dev-lifecycle (smoke test orchestration) | Project-specific tools | 3-category smoke test (health, core flow, resources), gate next stage |
| 7 | DOCUMENT | dev-lifecycle (documentation orchestration) | Mermaid (inline), canvas-design (optional) | Generate Mermaid architecture + sequence diagrams, update README/CLAUDE.md |
| 8 | RETROSPECT | dev-lifecycle (retrospective orchestration) | gsd-retrospective, ADR (gap check), work-log | Orchestrate retrospective, ADR gap check, work log, Living State update |
| 9 | PROMOTE | dev-lifecycle (optional promotion) | None | Skip-or-proceed flow, demo creation guidance |

2. **Skill Overlap Resolution table** -- add these rows:

| dev-lifecycle vs project tools in Stage 5 | dev-lifecycle is primary orchestrator providing the deploy checklist. Project-specific tools (Vercel CLI, Docker, etc.) are the execution tools. User runs deploy commands, Claude tracks progress. |
| dev-lifecycle vs gsd-retrospective in Stage 8 | dev-lifecycle is primary orchestrator. gsd-retrospective is the delegated skill that generates the retrospective document. dev-lifecycle coordinates the full flow (retro + ADR gap + work-log + Living State). |
| dev-lifecycle vs canvas-design in Stage 7 | dev-lifecycle uses Mermaid by default for all diagrams. canvas-design is optional enhancement if user requests visual design tools. |

Preserve all other content unchanged.
  </action>
  <verify>
    <automated>grep -q "dev-lifecycle (deploy orchestration)" skill/references/role-matrix.md && grep -q "dev-lifecycle (retrospective orchestration)" skill/references/role-matrix.md && grep -q "dev-lifecycle (optional promotion)" skill/references/role-matrix.md && grep -q "dev-lifecycle (smoke test orchestration)" skill/references/role-matrix.md && grep -q "dev-lifecycle (documentation orchestration)" skill/references/role-matrix.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>role-matrix.md shows dev-lifecycle as primary orchestrator for all 9 stages. Skill overlap resolution covers Stage 5, 7, and 8 relationships. Stage output types table unchanged (already correct).</done>
</task>

</tasks>

<verification>
SKILL.md has 9 Read directives (one per stage) and all stages show dev-lifecycle as primary.
role-matrix.md has dev-lifecycle in Primary Skill column for all 9 stages.

```bash
echo "=== SKILL.md Read directives ===" && grep 'Read:.*-stage.md' skill/SKILL.md
echo "=== role-matrix Stage 5-9 ===" && grep -E "^\| [5-9] " skill/references/role-matrix.md
```
</verification>

<success_criteria>
- SKILL.md Stage 5-9 sections follow Stage 1-4 format with Read directives
- role-matrix.md Stage 5-9 rows show dev-lifecycle as primary
- Skill overlap resolution table covers outer loop stage relationships
- No content lost from existing sections (State Management, Memory, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/09-outer-loop/09-02-SUMMARY.md`
</output>
