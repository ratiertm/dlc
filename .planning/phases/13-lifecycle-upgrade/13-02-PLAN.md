---
phase: 13-lifecycle-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - skill/SKILL.md
  - skill/references/stage-transitions.md
  - skill/templates/state.json
autonomous: true
requirements: [UPGR-01, UPGR-04]

must_haves:
  truths:
    - "Session Start includes a version check step between state load and Living State read"
    - "Version mismatch triggers Read of lifecycle-upgrade.md reference"
    - "state.json template reflects v2.0 schema (version 2.0, current.completeness field)"
    - "SKILL.md stays within 500-line budget (target: under 370 lines)"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Version check step in Session Start, Upgrade section"
      contains: "lifecycle-upgrade.md"
    - path: "skill/templates/state.json"
      provides: "v2.0 template for fresh installs"
      contains: "completeness"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/lifecycle-upgrade.md"
      via: "Read directive in Session Start"
      pattern: "lifecycle-upgrade\\.md"
    - from: "skill/SKILL.md"
      to: "skill/VERSION"
      via: "Version check reference"
      pattern: "VERSION"
    - from: "skill/SKILL.md"
      to: "skill/CHANGELOG.md"
      via: "Changelog mention in Upgrade section"
      pattern: "CHANGELOG"
---

<objective>
Wire the upgrade mechanism into SKILL.md session start and update the state.json template to v2.0 schema.

Purpose: Connect the migration infrastructure (Plan 01) to the runtime entry point so version mismatches are detected and handled automatically.
Output: Modified SKILL.md (version check in session start + Upgrade section), updated state.json template
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-lifecycle-upgrade/13-RESEARCH.md
@.planning/phases/13-lifecycle-upgrade/13-01-SUMMARY.md

@skill/SKILL.md
@skill/templates/state.json
@skill/references/stage-transitions.md

<interfaces>
<!-- Files created by Plan 01 that this plan references -->

From skill/VERSION:
```
2.0.0
```

From skill/CHANGELOG.md:
- Contains [2.0.0] and [1.0.0] version entries

From skill/references/lifecycle-upgrade.md:
- Migration algorithm triggered by version mismatch
- Backup -> Registry -> Marker -> Cleanup flow
- Expects to be Read when skill/VERSION > .lifecycle/.dlc-version

<!-- Current SKILL.md Session Start (lines 37-58) to be modified -->
From skill/SKILL.md Session Start:
```markdown
1. **Read state:** Load `.lifecycle/state.json`
   - If not exists AND `.lifecycle/` directory exists: **Reconcile** (Read: ...)
   - If not exists AND no `.lifecycle/`: Initialize from templates...
2. **Read Living State (if exists):** Load `.lifecycle/LIVING-STATE.md`
3. **Display position:** ...
4. **First run:** Detect project type...
5. **Check gates:** Verify manifest.json...
```

<!-- Current state.json template (v1.0) to be updated -->
From skill/templates/state.json:
```json
{
  "version": "1.0",
  "current": { "stage": 1, "stage_name": "PLAN", "status": "not_started", "feature": null, "mode": "feature" }
}
```
Note: Missing `current.completeness` field (added in Phase 12, must be in template now)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version check to SKILL.md Session Start and Upgrade section</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (full file -- need exact line numbers for Session Start steps and section placement)
    - skill/references/lifecycle-upgrade.md (created by Plan 01 -- verify it exists and confirm reference path)
    - skill/VERSION (created by Plan 01 -- confirm exists)
  </read_first>
  <action>
Modify skill/SKILL.md with two additions. SKILL.md is currently 355 lines. Budget: add no more than 15 lines (target: under 370).

**Addition 1: Version check step in Session Start (after step 1, before step 2)**

Insert step 1b between existing step 1 (Read state) and step 2 (Read Living State):

```markdown
1b. **Version check:** Compare `$CLAUDE_SKILL_DIR/VERSION` against `.lifecycle/.dlc-version`
   - If `.dlc-version` missing or older: Read `$CLAUDE_SKILL_DIR/references/lifecycle-upgrade.md` and run migration
   - If versions match: continue normally
```

This is 3 lines. Matches the exact pattern from 13-RESEARCH.md lines 296-309.

**Addition 2: Upgrade section in SKILL.md**

Add a brief Upgrade section AFTER the Edge Cases section (near the end, before the architecture audit HTML comment). This section provides top-level context without inlining migration logic:

```markdown
## Upgrade

When the skill version (`$CLAUDE_SKILL_DIR/VERSION`) is newer than the project's `.lifecycle/.dlc-version`, migration runs automatically during Session Start step 1b. Migration is safe: pre-migration backup enables rollback on failure.

Read: `$CLAUDE_SKILL_DIR/references/lifecycle-upgrade.md`
Read: `$CLAUDE_SKILL_DIR/CHANGELOG.md`
```

This is ~5 lines (header + 2 content lines + 2 Read directives).

**Total addition: ~8-10 lines. Final SKILL.md should be 363-365 lines.**

Do NOT inline migration logic. The Read directives point to lifecycle-upgrade.md which has all details.

Update the architecture audit HTML comment at the bottom to reflect:
- New line count (e.g., "SKILL.md 365 lines")
- Add "References: 17/17 files" (was 16, now includes lifecycle-upgrade.md -- but double-check the actual count first)
- Add "Audit date: 2026-03-25"
  </action>
  <verify>
    <automated>grep -n "Version check\|lifecycle-upgrade\.md\|\.dlc-version\|CHANGELOG\.md" skill/SKILL.md && echo "---" && wc -l skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "Version check" in Session Start section
    - SKILL.md contains "lifecycle-upgrade.md" Read directive
    - SKILL.md contains "CHANGELOG.md" Read directive
    - SKILL.md contains ".dlc-version" reference
    - SKILL.md contains "## Upgrade" section header
    - SKILL.md total lines is between 360 and 375 (under 500 budget, under 370 target preferred)
    - Session Start step numbering is correct (1, 1b, 2, 3, 4, 5)
  </acceptance_criteria>
  <done>SKILL.md has version check in Session Start step 1b and Upgrade section with Read directives, within line budget</done>
</task>

<task type="auto">
  <name>Task 2: Update state.json template to v2.0 schema and update state-reconcile defaults</name>
  <files>skill/templates/state.json, skill/references/state-reconcile.md</files>
  <read_first>
    - skill/templates/state.json (current v1.0 template)
    - skill/references/state-reconcile.md (references state.json version field)
    - .planning/phases/13-lifecycle-upgrade/13-RESEARCH.md (lines 200-260 for state.json v2.0 schema)
  </read_first>
  <action>
**Update skill/templates/state.json** to v2.0 schema:

```json
{
  "version": "2.0",
  "project": {
    "name": "",
    "type": "auto",
    "detected_type": null,
    "confirmed": false
  },
  "current": {
    "stage": 1,
    "stage_name": "PLAN",
    "status": "not_started",
    "feature": null,
    "mode": "feature",
    "completeness": null
  },
  "progress": {
    "stages_completed": [],
    "current_stage_started_at": null,
    "last_transition_at": null
  },
  "session": {
    "last_active": null,
    "resume_hint": ""
  }
}
```

Changes from v1.0:
- `version`: "1.0" -> "2.0"
- `current.completeness`: new field, null (Phase 12 addition)

**Update skill/references/state-reconcile.md** Step 5 (Reconstruct state.json):

In the reconstructed state.json template in Step 5, update:
- `"version": "1.0"` -> `"version": "2.0"`
- Add `"completeness": null` to the `current` object

This ensures that state reconcile produces v2.0-compatible state files.

Do NOT change any other fields or logic in state-reconcile.md. Only update the JSON template in Step 5.
  </action>
  <verify>
    <automated>grep '"version"' skill/templates/state.json && grep '"completeness"' skill/templates/state.json && grep -A2 '"version"' skill/references/state-reconcile.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - skill/templates/state.json contains `"version": "2.0"`
    - skill/templates/state.json contains `"completeness": null` inside current object
    - skill/references/state-reconcile.md Step 5 JSON template shows version "2.0"
    - skill/references/state-reconcile.md Step 5 JSON template includes completeness field
    - No other changes to state-reconcile.md logic (only the JSON template updated)
  </acceptance_criteria>
  <done>state.json template is v2.0 schema, state-reconcile produces v2.0-compatible files</done>
</task>

</tasks>

<verification>
- `grep "Version check" skill/SKILL.md` finds session start step
- `grep "lifecycle-upgrade.md" skill/SKILL.md` finds Read directive
- `grep "## Upgrade" skill/SKILL.md` finds Upgrade section
- `wc -l skill/SKILL.md` is under 375
- `grep '"2.0"' skill/templates/state.json` confirms v2.0 schema
- `grep '"completeness"' skill/templates/state.json` confirms new field
- `grep '"2.0"' skill/references/state-reconcile.md` confirms reconcile updated
</verification>

<success_criteria>
SKILL.md has version check wired into Session Start (step 1b) and a dedicated Upgrade section pointing to lifecycle-upgrade.md and CHANGELOG.md. state.json template is v2.0 schema. state-reconcile.md produces v2.0-compatible state. SKILL.md stays under 375 lines (well within 500 budget).
</success_criteria>

<output>
After completion, create `.planning/phases/13-lifecycle-upgrade/13-02-SUMMARY.md`
</output>
