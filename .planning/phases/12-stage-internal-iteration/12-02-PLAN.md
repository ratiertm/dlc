---
phase: 12-stage-internal-iteration
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - skill/references/completeness-scoring.md
  - skill/references/do-stage.md
  - skill/SKILL.md
  - skill/templates/living-state.md
autonomous: true
requirements: [ITER-03, ITER-04]
must_haves:
  truths:
    - "Each stage completion reports a Completeness score (N/10)"
    - "Completeness score is Claude's contextual judgment, not a formula"
    - "Decision points show per-option Completeness comparison"
    - "Living State Document displays current Completeness score"
    - "Completeness is informational, not a stage gate"
  artifacts:
    - path: "skill/references/completeness-scoring.md"
      provides: "Full Completeness scoring logic, dimensions, display format, decision comparison"
      min_lines: 60
    - path: "skill/SKILL.md"
      provides: "Brief Completeness and mini-verify mentions with Read directives"
      contains: "Completeness"
    - path: "skill/templates/living-state.md"
      provides: "Completeness score field in Current State section"
      contains: "Completeness"
  key_links:
    - from: "skill/references/completeness-scoring.md"
      to: "skill/references/do-stage.md"
      via: "DO stage completion calls Completeness scoring"
      pattern: "Completeness.*N/10"
    - from: "skill/SKILL.md"
      to: "skill/references/completeness-scoring.md"
      via: "Read directive"
      pattern: "completeness-scoring.md"
    - from: "skill/templates/living-state.md"
      to: "state.json"
      via: "Completeness from current.completeness"
      pattern: "Completeness"
---

<objective>
Create Completeness scoring system and integrate mini-verify + Completeness into SKILL.md.

Purpose: Give users continuous quality awareness through N/10 scores at stage completion and informed decision-making through per-option Completeness comparison.
Output: New completeness-scoring.md reference, updated SKILL.md with brief mentions, updated living-state.md template, updated do-stage.md Step 5 with Completeness display.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stage-internal-iteration/12-CONTEXT.md
@.planning/phases/12-stage-internal-iteration/12-RESEARCH.md
@.planning/phases/12-stage-internal-iteration/12-01-SUMMARY.md

<interfaces>
<!-- From SKILL.md (353 lines, budget 500): -->
Stage 2 DO section is at approximately lines 77-93.
The Configuration section is at approximately lines 281-291.
Budget remaining: ~147 lines. Phase 12 additions must stay under ~15 lines in SKILL.md.

<!-- From living-state.md template: -->
```markdown
## Current State

- Inner loop progress: {stages completed summary}
- Deviations: {count and brief summary}
- Key blockers: {active blockers or "none"}
```

<!-- From do-stage.md Step 5d (after Plan 01 changes): -->
Step 5 displays final checklist with mini-verify stats. Completeness score adds one more display element here.

<!-- From 12-CONTEXT.md locked decisions: -->
- Completeness: Claude judges N/10 per stage, not formula-based
- Considers: artifact completeness, spec step coverage, deviation count, verification pass rate
- Decision display: "Option A: 6/10 (missing error handling) vs Option B: 9/10 (full coverage)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create completeness-scoring.md reference</name>
  <files>skill/references/completeness-scoring.md</files>
  <read_first>
    - skill/references/do-stage.md
    - skill/references/test-stage.md
    - skill/references/stage-transitions.md
    - .planning/phases/12-stage-internal-iteration/12-RESEARCH.md
    - .planning/phases/12-stage-internal-iteration/12-CONTEXT.md
  </read_first>
  <action>
Create new file `skill/references/completeness-scoring.md` with the following content structure:

```markdown
# Completeness Scoring Reference

Cross-stage quality scoring system. Claude assesses Completeness as a contextual N/10 judgment at every stage completion and at decision points.

## When This Runs

- At the completion of each stage (after all stage-specific work, before the completion announcement)
- At any decision point where multiple options are presented to the user

## Stage Completion Score

At the completion of each stage, before the completion announcement, assess Completeness:

**Dimensions (contextual weighting, not formula):**

| Dimension | What to Consider | Applies To |
|-----------|-----------------|------------|
| Artifact completeness | Are all expected outputs present and well-formed? | All stages |
| Spec step coverage | What percentage of steps are verified vs implemented vs failed? | DO, TEST |
| Deviation count | How many deviations from original spec? | DO, TEST |
| Verification pass rate | Mini-verify successes vs failures, retry counts | DO |
| Edge case coverage | Are error conditions handled? Validation complete? | TEST |
| Traceability | SPEC comments present? ADRs linked? | COMMIT |
| Documentation coverage | Architecture, sequence diagrams, README current? | DOCUMENT |

**Score:** N/10 -- Claude's contextual judgment considering relevant dimensions for the current stage. This is NOT a formula. Do not create weighted averages or point systems.

**Display at stage completion (add to each stage's completion announcement):**

```
Stage {N} {NAME} -- COMPLETE
Completeness: {N}/10 ({brief justification referencing key dimensions})
```

**Examples:**
- DO: `Completeness: 8/10 (all 5 steps verified, 1 deviation, 2 retries needed)`
- TEST: `Completeness: 9/10 (all steps pass, prototype match 100%, 1 traceability warning)`
- COMMIT: `Completeness: 7/10 (all files staged, 2/5 SPEC comments missing)`

## Decision Point Comparison

When presenting options at any decision point during any stage, include a Completeness projection for each option:

**Format:**
```
Option A: {N}/10 ({brief quality assessment})
Option B: {N}/10 ({brief quality assessment})
Option C: {N}/10 ({brief quality assessment})
```

**Examples:**
```
Option A: 6/10 (happy path only, missing error handling for 3 conditions)
Option B: 9/10 (full coverage including edge cases, adds ~30min)
Option C: 7/10 (covers errors but skips loading states)
```

**When to show comparison:**
- When user must choose between implementation approaches
- When deviation handling presents alternatives
- When TEST stage failure offers fix vs accept vs revise options
- NOT for trivial choices (variable naming, formatting)

## State Persistence

Store Completeness in `state.json` after each stage completion:

```json
{
  "current": {
    "completeness": {
      "score": 8,
      "reason": "all steps verified, 1 deviation, 2 retries needed",
      "stage": "DO",
      "assessed_at": "{ISO-8601}"
    }
  }
}
```

LIVING-STATE.md reads `current.completeness` during regeneration to display the score.

## Anti-Patterns

| # | Anti-Pattern | Why It Is Wrong |
|---|-------------|-----------------|
| 1 | **Formula-based scoring** | User explicitly decided against formulas. Claude judges holistically. Do not create weighted averages or point systems. |
| 2 | **Completeness as a gate** | Completeness is informational. Existing gate conditions (manifest.json artifact checks) are the ONLY blockers. A low score does not prevent stage transition. |
| 3 | **Inflated scores** | Be honest. 10/10 means genuinely complete with no gaps. Most real implementations score 7-9/10. |
| 4 | **Scores without justification** | Always include a brief parenthetical explaining the key factors. A bare number is not useful. |
```

Target: 80-100 lines. Keep it focused on the scoring logic, display format, and integration points.
  </action>
  <verify>
    <automated>test -f /Users/leeeunmi/Projects/active/dev-lifecycle/skill/references/completeness-scoring.md && grep -c "Completeness\|N/10\|Decision Point\|Anti-Pattern" /Users/leeeunmi/Projects/active/dev-lifecycle/skill/references/completeness-scoring.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at skill/references/completeness-scoring.md
    - grep "Stage Completion Score" skill/references/completeness-scoring.md returns 1 match
    - grep "Decision Point Comparison" skill/references/completeness-scoring.md returns 1 match
    - grep "NOT a formula" skill/references/completeness-scoring.md returns 1 match (or similar anti-formula statement)
    - grep "state.json" skill/references/completeness-scoring.md returns at least 1 match (persistence)
    - grep "Anti-Pattern" skill/references/completeness-scoring.md returns at least 1 match
    - grep "informational" skill/references/completeness-scoring.md returns at least 1 match (not a gate)
    - File is between 60-120 lines (wc -l)
  </acceptance_criteria>
  <done>completeness-scoring.md exists with stage completion scoring (N/10 contextual judgment with dimension table), decision point comparison format with examples, state.json persistence format, and anti-patterns preventing formula-based scoring and gate behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Completeness into do-stage.md, SKILL.md, and living-state.md</name>
  <files>skill/references/do-stage.md, skill/SKILL.md, skill/templates/living-state.md</files>
  <read_first>
    - skill/references/do-stage.md
    - skill/SKILL.md
    - skill/templates/living-state.md
    - skill/references/completeness-scoring.md
  </read_first>
  <action>
Three targeted edits to wire Completeness scoring into the existing system:

**Edit 1: do-stage.md Step 5 -- Add Completeness assessment**

After the current Step 5d (Display final checklist) and before Step 5e (Announce), insert a new sub-step:

```markdown
**d.5. Assess and display Completeness score:**

Read: `$CLAUDE_SKILL_DIR/references/completeness-scoring.md`

Evaluate the DO stage output quality considering: spec step coverage (verified vs implemented vs failed), deviation count, mini-verify retry count, and edge case handling. Display as part of the completion announcement:

```
Completeness: {N}/10 ({brief justification})
```

Record in state.json: `current.completeness = { "score": N, "reason": "{justification}", "stage": "DO", "assessed_at": "{ISO-8601}" }`
```

Also update the completion display block to include the Completeness line (add it after the `Mini-verify:` line in the display format).

**Edit 2: SKILL.md Stage 2 DO section -- Add mini-verify and Completeness mentions**

In the Stage 2: DO section (approximately lines 77-93), add these lines to the Pipeline list, keeping total addition under 8 lines:

After the existing pipeline item `3. Per-step implementation (implement each chain step, add SPEC comments)`, add:
```
   3a. Mini-verify each step (execution-level check, controlled by verification_strictness config)
```

After the existing pipeline item `6. Completion (artifact registration, state update)`, modify to:
```
6. Completion (artifact registration, Completeness score, state update)
```

Add a Read directive for completeness-scoring.md. Find a suitable location -- either in the Stage 2 section or as a general reference. Add:
```
Read: `$CLAUDE_SKILL_DIR/references/completeness-scoring.md`
```

This should be placed after the existing `Read: $CLAUDE_SKILL_DIR/references/do-stage.md` line in Stage 2.

**CRITICAL: SKILL.md total additions must not exceed 15 lines.** Count carefully. Current: 353 lines. After: must be <= 368 lines (well under 500 budget).

**Edit 3: living-state.md template -- Add Completeness field**

In the `## Current State` section of `skill/templates/living-state.md`, add a Completeness line:

Change:
```markdown
## Current State

- Inner loop progress: {stages completed summary}
- Deviations: {count and brief summary}
- Key blockers: {active blockers or "none"}
```

To:
```markdown
## Current State

- Inner loop progress: {stages completed summary}
- Completeness: {N}/10 ({brief justification})
- Deviations: {count and brief summary}
- Key blockers: {active blockers or "none"}
```

Also update the generation instructions comment at the bottom to include:
```
- state.json current.completeness -> Completeness score
```
  </action>
  <verify>
    <automated>grep -c "Completeness" /Users/leeeunmi/Projects/active/dev-lifecycle/skill/SKILL.md /Users/leeeunmi/Projects/active/dev-lifecycle/skill/templates/living-state.md /Users/leeeunmi/Projects/active/dev-lifecycle/skill/references/do-stage.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Completeness" skill/SKILL.md returns at least 1 match
    - grep "completeness-scoring.md" skill/SKILL.md returns at least 1 match (Read directive)
    - grep "Mini-verify\|mini-verify" skill/SKILL.md returns at least 1 match
    - wc -l skill/SKILL.md shows <= 370 (budget check: 353 + max 15 = 368, allowing 2 line margin)
    - grep "Completeness" skill/templates/living-state.md returns at least 1 match
    - grep "current.completeness" skill/templates/living-state.md returns at least 1 match (in generation instructions)
    - grep "d.5.*Completeness\|Completeness.*score" skill/references/do-stage.md returns at least 1 match
    - grep "completeness-scoring.md" skill/references/do-stage.md returns at least 1 match (Read directive)
  </acceptance_criteria>
  <done>SKILL.md contains mini-verify pipeline mention and Completeness Read directive (under 15 lines added, total under 370). living-state.md template shows Completeness field in Current State. do-stage.md Step 5 includes Completeness assessment sub-step with state.json persistence.</done>
</task>

</tasks>

<verification>
1. `test -f skill/references/completeness-scoring.md` -- new reference exists
2. `grep "completeness-scoring.md" skill/SKILL.md skill/references/do-stage.md` -- Read directives in both files
3. `grep "Completeness" skill/templates/living-state.md` -- template updated
4. `wc -l skill/SKILL.md` -- under 370 lines (ARCH-01 budget compliance)
5. `grep -c "Completeness\|completeness" skill/references/completeness-scoring.md` -- substantial content (>10 mentions)
6. No formula-based scoring anywhere: `grep -i "weighted\|average\|formula\|calculate" skill/references/completeness-scoring.md` returns 0 matches (except in anti-patterns)
</verification>

<success_criteria>
- completeness-scoring.md is a self-contained reference covering stage completion scoring, decision comparison, state persistence, and anti-patterns
- SKILL.md additions are minimal (under 15 lines): mini-verify pipeline mention + Completeness Read directive
- SKILL.md total line count stays under 370 (well within 500 budget)
- living-state.md template includes Completeness field that reads from state.json
- do-stage.md Step 5 includes Completeness assessment before completion announcement
- Completeness is explicitly informational (not a gate) throughout all files
- Decision point comparison format matches CONTEXT.md locked decision format
</success_criteria>

<output>
After completion, create `.planning/phases/12-stage-internal-iteration/12-02-SUMMARY.md`
</output>
