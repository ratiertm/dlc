---
phase: 12-stage-internal-iteration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/references/do-stage.md
  - skill/templates/spec-template.md
autonomous: true
requirements: [ITER-01, ITER-02]
must_haves:
  truths:
    - "After implementing each spec step in DO, a mini-verify check runs immediately"
    - "When mini-verify fails, DO loops on that step (QA->Fix->Verify) up to 3 times"
    - "After 3 failures, user is presented with options and DO stops on that step"
    - "verification_strictness config setting controls mini-verify depth"
    - "Spec step Status field shows verified (attempt N) or failed (3 attempts)"
  artifacts:
    - path: "skill/references/do-stage.md"
      provides: "Mini-verify sub-step 2d.1 and retry loop sub-step 2d.2"
      contains: "Step 2d.1"
    - path: "skill/templates/spec-template.md"
      provides: "Updated Status field values including verified and failed formats"
      contains: "verified"
  key_links:
    - from: "skill/references/do-stage.md"
      to: "skill/references/lifecycle-config.md"
      via: "verification_strictness resolution"
      pattern: "verification_strictness"
    - from: "skill/references/do-stage.md"
      to: "skill/templates/spec-template.md"
      via: "Status field format"
      pattern: "verified \\(attempt"
---

<objective>
Add mini-verify loop and retry logic to the DO stage adapter.

Purpose: Catch implementation issues immediately during DO (not deferred to TEST) by executing verification after each spec step, with automatic retry on failure.
Output: Updated do-stage.md with Step 2d.1 (mini-verify) and Step 2d.2 (retry loop), updated spec-template.md with new Status values.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stage-internal-iteration/12-CONTEXT.md
@.planning/phases/12-stage-internal-iteration/12-RESEARCH.md

<interfaces>
<!-- Key patterns from existing files that executor needs -->

From skill/references/do-stage.md Step 2 (lines 78-113):
Current flow is: 2a (read step) -> 2b (implement) -> 2c (SPEC comment) -> 2d (update status to "implemented") -> 2e (re-display checklist) -> 2f (deviation check).
The mini-verify inserts between 2d and 2e as sub-steps 2d.1 and 2d.2.

From skill/references/lifecycle-config.md:
```
verification_strictness | string | standard | strict|standard|relaxed
```
Resolution: env LIFECYCLE_VERIFICATION_STRICTNESS > .lifecycle/config.yaml > default "standard"

From skill/references/e2e-spec.md Status lifecycle:
```
pending --(DO implements)--> implemented --(TEST verifies)--> verified
                                              |
                                              └--(TEST fails)--> failed
```
Phase 12 extends this: implemented -> verified (attempt N) happens in DO via mini-verify, not just in TEST.

From skill/templates/spec-template.md:
```
**Status:** pending
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mini-verify and retry loop to do-stage.md Step 2</name>
  <files>skill/references/do-stage.md</files>
  <read_first>
    - skill/references/do-stage.md
    - skill/references/lifecycle-config.md
    - skill/references/e2e-spec.md
    - .planning/phases/12-stage-internal-iteration/12-RESEARCH.md
  </read_first>
  <action>
Modify `skill/references/do-stage.md` to add mini-verify after step 2d and a retry loop. The changes are:

**1. Insert Step 2d.1 (Mini-Verify) after current step 2d, before step 2e.**

Add this content after the line `- Write the change to disk immediately` (end of step 2d) and before `**e. Re-display the checklist`:

```markdown
**d.1. Mini-verify the step:**

Resolve `verification_strictness` using lifecycle-config resolution (env `LIFECYCLE_VERIFICATION_STRICTNESS` > `.lifecycle/config.yaml` > default `standard`):

| Config Value | Mini-Verify Behavior | Status After |
|-------------|---------------------|-------------|
| `strict` | Execute verification command. Must pass. No override. | `verified (attempt N)` or `failed (3 attempts)` |
| `standard` | Execute verification command. Must pass. User can override after at least 1 retry. | `verified (attempt N)` or `failed (3 attempts)` or `implemented (override: {justification})` |
| `relaxed` | Skip execution. Code existence check only. | Status stays `implemented`, continue to 2e |

If `relaxed`: skip mini-verify, continue to step 2e.

Otherwise, determine verification command from `state.json` `project_type`:

| Project Type | Verification Command | Success Signal |
|-------------|---------------------|----------------|
| node/react/next | `npm test -- --bail` or `npm run build` | exit code 0 |
| python | `python -m pytest {test_file} -x` or `python -c "import {module}"` | exit code 0 |
| flutter | `flutter test {test_file}` or `flutter analyze` | exit code 0 |
| rust | `cargo check` or `cargo test {test_name}` | exit code 0 |
| go | `go build ./...` or `go test ./{pkg}` | exit code 0 |
| generic | Attempt build/compile command from project config | exit code 0 |

Choose the most targeted command for the specific step:
- Screen/Response steps: build/compile check
- Connection/Processing steps: unit test if available, else build
- Error steps: specific error case test if available
- Timeout: 30 seconds max

Execute and evaluate:
- Run the verification command
- Capture: exit code, stdout (last 20 lines), stderr (last 20 lines)
- Success = exit code 0 AND no error patterns in output
- On success: update spec.md status to `**Status:** verified (attempt 1)`, continue to step 2e
- On failure: enter retry loop (step 2d.2)
```

**2. Insert Step 2d.2 (Retry Loop) immediately after Step 2d.1:**

```markdown
**d.2. Retry loop (on mini-verify failure):**

On mini-verify failure, enter the QA->Fix->Verify loop (max 3 attempts):

1. **Diagnose:** Analyze error output from verification command
   - Identify the specific failure (compile error, test assertion, runtime error)
   - Scope the fix to the failing step's code ONLY (do not modify other steps' files)

2. **Fix:** Apply targeted fix to the failing code
   - Modify ONLY files related to the current spec step
   - Keep SPEC comment in place

3. **Re-verify:** Run the same verification command again
   - If pass: update status `**Status:** verified (attempt {N})`, continue to 2e
   - If fail: increment attempt, continue loop

4. **After 3 failures:** STOP retrying
   - Update status: `**Status:** failed (3 attempts)`
   - Report to user:
     ```
     Mini-verify FAILED after 3 attempts
     =====================================
     Step: e2e-{feature}-{NNN} ({title})
     Last error: {error summary}
     Attempts: 3/3

     The step's code is implemented but verification fails.
     Options:
       1. I'll investigate and fix manually, then resume
       2. Skip verification for this step (mark as implemented)
       3. Revise the spec step if requirements changed
     ```
   - Wait for user direction before continuing to next step

Note for `standard` strictness: user can also say "skip verification" after at least 1 retry attempt. Status becomes `**Status:** implemented (override: {justification})`.
```

**3. Update Step 5 (Completion) trigger condition.**

Change line 224: `**Trigger:** All spec steps have **Status:** implemented.`
To: `**Trigger:** All spec steps have **Status:** verified, implemented, or implemented (override). No step has **Status:** failed or pending.`

**4. Update Step 5d (Display final checklist) to show verification status.**

Update the checklist display format in Step 5d to show the new status values:

```
DO Stage -- {feature-name} -- COMPLETE
=======================================
Spec: .lifecycle/features/{feature-name}/spec.md
Status: implementing (M/M steps done)

Checklist:
  [x] e2e-{feature}-001: {title} -- verified (attempt 1)
  [x] e2e-{feature}-002: {title} -- verified (attempt 2)
  [x] e2e-{feature}-003: {title} -- implemented (override: build timeout)
  ...
Deviations: N recorded
ADRs created: N
Mini-verify: {passed}/{total} steps verified, {retries} total retries
```

**5. Add anti-pattern entry** to the Anti-Patterns table at the bottom:

| 7 | **Retrying with the same approach** | Each retry must analyze the specific error and try a different fix. Repeating identical code wastes all 3 attempts. |
| 8 | **Mini-verify running full test suite** | Always prefer targeted commands (single test file, single module compile). Timeout is 30 seconds -- full suites will exceed this. |
| 9 | **Retry loop modifying unrelated code** | Fixes must be scoped to the current spec step's files. If the error requires changes outside the step's scope, escalate to user. |

**Important:** Do NOT use `sed`. Read the full file, make the insertions at the correct positions, and write the complete updated file.
  </action>
  <verify>
    <automated>grep -c "Step 2d.1\|Step 2d.2\|retry loop\|mini-verify\|verified (attempt\|failed (3 attempts)" /Users/leeeunmi/Projects/active/dev-lifecycle/skill/references/do-stage.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Step 2d.1" skill/references/do-stage.md returns at least 1 match
    - grep "Step 2d.2" skill/references/do-stage.md returns at least 1 match
    - grep "verification_strictness" skill/references/do-stage.md returns at least 3 matches (table + resolution + note)
    - grep "verified (attempt" skill/references/do-stage.md returns at least 2 matches
    - grep "failed (3 attempts)" skill/references/do-stage.md returns at least 2 matches
    - grep "Retrying with the same approach" skill/references/do-stage.md returns 1 match
    - grep "implemented (override" skill/references/do-stage.md returns at least 1 match
    - The file follows existing markdown formatting conventions (## for steps, ** for sub-steps)
  </acceptance_criteria>
  <done>do-stage.md contains Step 2d.1 (mini-verify) with verification_strictness mapping table and project_type command table, Step 2d.2 (retry loop) with 3-attempt max and user escalation, updated Step 5 trigger and display format, and 3 new anti-pattern entries.</done>
</task>

<task type="auto">
  <name>Task 2: Update spec-template.md Status field to include new values</name>
  <files>skill/templates/spec-template.md</files>
  <read_first>
    - skill/templates/spec-template.md
    - skill/references/e2e-spec.md
  </read_first>
  <action>
Update `skill/templates/spec-template.md` to document the extended Status lifecycle that includes mini-verify outcomes.

**1. Find the `**Status:** pending` lines in each chain step template section.** Add a comment after the first occurrence (near the top of the template, after the frontmatter section or in a Status reference block) explaining all valid Status values:

```markdown
<!-- Status values:
  pending        -- Not yet implemented
  implemented    -- Code written, not yet verified
  verified (attempt N) -- Mini-verify passed during DO (N = attempt number)
  implemented (override: {reason}) -- Mini-verify skipped with user justification
  failed (N attempts) -- Mini-verify failed after N retry attempts
  verified       -- TEST stage verification passed
  failed         -- TEST stage verification failed
-->
```

**2. Leave existing `**Status:** pending` lines unchanged** -- they are correct as the initial status for new specs.

**3. Update the Step-level status lifecycle in e2e-spec.md** (if a Status lifecycle diagram exists in spec-template.md). The new lifecycle is:

```
pending --(DO implements)--> implemented --(mini-verify pass)--> verified (attempt N)
                                |                                      |
                                +--(mini-verify fail x3)--> failed (3 attempts)
                                |                                      |
                                +--(user override)--> implemented (override: {reason})
                                                                       |
                                               All three continue to TEST stage:
                                               --(TEST verifies)--> verified
                                               --(TEST fails)--> failed
```

This is documentation only. Do NOT change the `**Status:** pending` default in any chain step section.
  </action>
  <verify>
    <automated>grep -c "verified (attempt\|implemented (override\|failed.*attempts" /Users/leeeunmi/Projects/active/dev-lifecycle/skill/templates/spec-template.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "verified (attempt N)" skill/templates/spec-template.md returns at least 1 match
    - grep "implemented (override" skill/templates/spec-template.md returns at least 1 match
    - grep "failed.*attempts" skill/templates/spec-template.md returns at least 1 match
    - grep "Status.*pending" skill/templates/spec-template.md still returns matches (existing pending lines preserved)
    - The file does not exceed 150 lines (currently 125, adding ~15-20 lines of Status reference)
  </acceptance_criteria>
  <done>spec-template.md contains a Status values reference comment documenting all valid values including verified (attempt N), implemented (override: reason), and failed (N attempts). Existing pending defaults are unchanged.</done>
</task>

</tasks>

<verification>
1. `grep -c "Step 2d" skill/references/do-stage.md` returns >= 4 (2d.1 heading + content, 2d.2 heading + content)
2. `grep "verification_strictness" skill/references/do-stage.md` shows strictness mapping table
3. `grep "verified (attempt" skill/references/do-stage.md skill/templates/spec-template.md` shows matches in both files
4. `wc -l skill/references/do-stage.md` shows increase of ~80-100 lines from current 318
5. Mini-verify flow is coherent: 2a->2b->2c->2d->2d.1->(pass: 2e, fail: 2d.2)->2e
</verification>

<success_criteria>
- do-stage.md Step 2 includes mini-verify (2d.1) and retry loop (2d.2) with all tables and formats from research
- verification_strictness correctly mapped: strict=must pass, standard=pass or override, relaxed=skip
- Retry loop capped at 3 attempts with user escalation on failure
- Step 5 completion trigger updated to account for verified/implemented/override statuses
- spec-template.md documents all new Status values
- Both files maintain existing formatting conventions
</success_criteria>

<output>
After completion, create `.planning/phases/12-stage-internal-iteration/12-01-SUMMARY.md`
</output>
