---
phase: 08-memory-decision-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/templates/settings-changelog.md
  - skill/templates/decisions.md
  - skill/templates/living-state.md
autonomous: true
requirements: [MEMO-01, MEMO-02, MEMO-03]

must_haves:
  truths:
    - "Settings changelog template exists with table format (Date | File | Change | Reason)"
    - "Decision log template exists with table format (Date | Decision | Reason)"
    - "Living State template exists with all 6 required sections (Current State, Active Settings, Recent Decisions, Event Timeline, Key ADRs, Resume Hint)"
  artifacts:
    - path: "skill/templates/settings-changelog.md"
      provides: "Reusable settings changelog starter"
      contains: "Settings Changelog"
    - path: "skill/templates/decisions.md"
      provides: "Reusable lightweight decision log starter"
      contains: "Decision Log"
    - path: "skill/templates/living-state.md"
      provides: "Reusable Living State Document starter"
      contains: "Living State"
  key_links:
    - from: "skill/templates/living-state.md"
      to: "skill/templates/settings-changelog.md"
      via: "Active Settings section aggregates from settings-changelog"
      pattern: "Active Settings"
    - from: "skill/templates/living-state.md"
      to: "skill/templates/decisions.md"
      via: "Recent Decisions section aggregates from decisions.md"
      pattern: "Recent Decisions"
---

<objective>
Create the three memory artifact templates (settings changelog, lightweight decision log, Living State Document) that form the foundation of Phase 8's memory system.

Purpose: These templates define the file formats that will be instantiated in each project's `.lifecycle/` directory. They are the reusable starters -- Plan 02 will wire update triggers into stage adapters.
Output: Three template files in `skill/templates/`
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-memory-decision-trail/08-CONTEXT.md
@.planning/phases/08-memory-decision-trail/08-RESEARCH.md

<interfaces>
<!-- Existing templates in skill/templates/ that set the pattern for new templates -->

From skill/templates/state.json:
```json
{
  "session": {
    "last_active": null,
    "resume_hint": ""
  }
}
```
Session fields in state.json are the source data for Living State's Resume Hint section.

From skill/references/stage-transitions.md (Transition Procedure Step 5):
```markdown
5. **Write history entry** to `.lifecycle/history/` as a timestamped JSON file
```
History entries feed into Living State's Event Timeline section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings changelog and decision log templates</name>
  <files>skill/templates/settings-changelog.md, skill/templates/decisions.md</files>
  <read_first>
    - skill/templates/state.json (existing template pattern)
    - .planning/phases/08-memory-decision-trail/08-RESEARCH.md (exact formats in "Code Examples" section)
  </read_first>
  <action>
Create two template files following the formats defined in 08-RESEARCH.md:

**skill/templates/settings-changelog.md:**
- Title: `# Settings Changelog`
- Description paragraph: "Append-only record of all settings and configuration changes. Each entry captures what changed and why, so future sessions never need to re-explain a settings decision."
- Usage section: "Copy to `.lifecycle/settings-changelog.md` on first settings change detection."
- Table header: `| Date | File | Change | Reason |` with separator row
- One example row: `| 2026-01-01 12:00 | .env | Added DB_HOST=localhost | Initial database setup |`
- Rules section with 3 rules:
  1. "Append only -- never edit or remove existing entries"
  2. "One row per change, even if multiple changes happen in one session"
  3. "Settings files include: .env*, *.config.*, package.json (scripts/dependencies), tsconfig.json, *.yaml/*.toml config files, framework config files"

**skill/templates/decisions.md:**
- Title: `# Decision Log`
- Description paragraph: "Lightweight one-line decision entries. For decisions that don't meet the ADR threshold (2+ viable approaches AND lasting consequences) but still deserve recording. Direction changes, settings rationale, small tradeoffs."
- Usage section: "Copy to `.lifecycle/decisions.md` on first lightweight decision."
- Table header: `| Date | Decision | Reason |` with separator row
- One example row: `| 2026-01-01 | Use Zustand over Redux for state management | Simpler API, no boilerplate |`
- One example ADR cross-reference row: `| 2026-01-01 | See ADR-001 | Database migration strategy (promoted to ADR) |`
- Rules section with 3 rules:
  1. "Append only -- never edit or remove existing entries"
  2. "If a decision meets BOTH ADR criteria (2+ viable approaches AND lasting consequences), create an ADR instead and add a cross-reference row here"
  3. "Keep entries to one line. If you need more than 2 lines, it's probably an ADR"
  </action>
  <verify>
    <automated>grep -c "Settings Changelog" skill/templates/settings-changelog.md && grep -c "| Date | File | Change | Reason |" skill/templates/settings-changelog.md && grep -c "Decision Log" skill/templates/decisions.md && grep -c "| Date | Decision | Reason |" skill/templates/decisions.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Settings Changelog" skill/templates/settings-changelog.md returns match
    - grep "| Date | File | Change | Reason |" skill/templates/settings-changelog.md returns match
    - grep "Append only" skill/templates/settings-changelog.md returns match
    - grep "Decision Log" skill/templates/decisions.md returns match
    - grep "| Date | Decision | Reason |" skill/templates/decisions.md returns match
    - grep "ADR" skill/templates/decisions.md returns match (cross-reference rule)
  </acceptance_criteria>
  <done>Both template files exist with correct table formats, example rows, and usage rules</done>
</task>

<task type="auto">
  <name>Task 2: Create Living State Document template</name>
  <files>skill/templates/living-state.md</files>
  <read_first>
    - .planning/phases/08-memory-decision-trail/08-RESEARCH.md (Living State full example in "Code Examples" section)
    - .planning/phases/08-memory-decision-trail/08-CONTEXT.md (locked decisions about Living State content)
    - skill/templates/state.json (session fields that Living State reads)
  </read_first>
  <action>
Create the Living State Document template following the structure from 08-RESEARCH.md and honoring locked decisions from 08-CONTEXT.md.

**skill/templates/living-state.md:**

Header block (placeholders):
```
# Living State

**Updated:** {timestamp}
**Feature:** {current feature name}
**Stage:** {N} {NAME} -- {status}
**Mode:** {execution mode}
```

Six required sections (per CONTEXT.md locked decision: "current state + recent decisions + active settings list + major event summary from project start"):

**## Current State**
- Inner loop progress placeholder
- Deviation count placeholder
- Key blockers placeholder

**## Active Settings**
Table: `| Setting | Value | Changed | Reason |`
Note: "Populated from last 20 entries of `.lifecycle/settings-changelog.md`"

**## Recent Decisions**
Table: `| Date | Decision | Reason |`
Note: "Populated from last 10 entries of `.lifecycle/decisions.md`"

**## Event Timeline**
Table: `| Date | Event |`
Note: "Populated from `.lifecycle/history/` JSON entries and `manifest.json` stage completions"

**## Key ADRs**
Bulleted list placeholder.
Note: "Active ADRs from `docs/decisions/` with one-line summaries"

**## Resume Hint**
Placeholder.
Note: "Copied from `state.json` `session.resume_hint`"

Add a generation instructions section at the bottom:
```
<!-- GENERATION INSTRUCTIONS (for Claude, not for display)
Update triggers:
1. Every stage transition (read stage-transitions.md)
2. Every session end
Sources:
- state.json -> Current State, header fields, Resume Hint
- settings-changelog.md -> Active Settings (last 20)
- decisions.md -> Recent Decisions (last 10)
- .lifecycle/history/ -> Event Timeline
- docs/decisions/ -> Key ADRs
- manifest.json -> stage completions for Event Timeline
Target size: ~100 lines max. Truncate older entries if needed.
-->
```

Usage section at top: "Copy to `.lifecycle/LIVING-STATE.md` at project initialization. Regenerate (overwrite) at every stage transition and session end."
  </action>
  <verify>
    <automated>grep -c "^## " skill/templates/living-state.md | xargs test 6 -le && echo "OK: 6+ sections" || echo "FAIL: less than 6 sections"; grep "Active Settings" skill/templates/living-state.md && grep "Recent Decisions" skill/templates/living-state.md && grep "Event Timeline" skill/templates/living-state.md && grep "Resume Hint" skill/templates/living-state.md && grep "settings-changelog" skill/templates/living-state.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Living State" skill/templates/living-state.md returns match
    - grep -c "^## " skill/templates/living-state.md returns 6 or more
    - grep "Active Settings" skill/templates/living-state.md returns match
    - grep "Recent Decisions" skill/templates/living-state.md returns match
    - grep "Event Timeline" skill/templates/living-state.md returns match
    - grep "Key ADRs" skill/templates/living-state.md returns match
    - grep "Resume Hint" skill/templates/living-state.md returns match
    - grep "settings-changelog" skill/templates/living-state.md returns match (source reference)
    - grep "decisions.md" skill/templates/living-state.md returns match (source reference)
    - grep "state.json" skill/templates/living-state.md returns match (source reference)
  </acceptance_criteria>
  <done>Living State template exists with all 6 sections, generation instructions, source file references, and ~100 line target note</done>
</task>

</tasks>

<verification>
All three template files exist in skill/templates/ with correct formats:
- `skill/templates/settings-changelog.md` -- append-only table with Date|File|Change|Reason
- `skill/templates/decisions.md` -- append-only table with Date|Decision|Reason
- `skill/templates/living-state.md` -- 6-section aggregation document with source references
</verification>

<success_criteria>
- Three new template files created in skill/templates/
- Settings changelog has table format matching research specification
- Decision log has table format with ADR cross-reference rule
- Living State has all 6 required sections per CONTEXT.md locked decisions
- Living State references its source files (state.json, settings-changelog.md, decisions.md)
- All templates include usage instructions for `.lifecycle/` instantiation
</success_criteria>

<output>
After completion, create `.planning/phases/08-memory-decision-trail/08-01-SUMMARY.md`
</output>
