---
phase: 08-memory-decision-trail
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - skill/references/stage-transitions.md
  - skill/references/do-stage.md
  - skill/references/commit-stage.md
  - skill/SKILL.md
autonomous: true
requirements: [MEMO-01, MEMO-02, MEMO-03, MEMO-04]

must_haves:
  truths:
    - "Stage transition procedure includes Living State regeneration step"
    - "DO stage completion includes settings changelog and decision log append steps"
    - "COMMIT stage completion includes settings changelog append step"
    - "SKILL.md Session Start reads Living State as primary context file"
    - "WHY+SEE cross-reference is documented in SKILL.md with link to do-stage.md"
  artifacts:
    - path: "skill/references/stage-transitions.md"
      provides: "Living State update trigger on every transition"
      contains: "LIVING-STATE"
    - path: "skill/references/do-stage.md"
      provides: "Memory file update triggers at DO completion"
      contains: "settings-changelog"
    - path: "skill/references/commit-stage.md"
      provides: "Settings changelog trigger at COMMIT completion"
      contains: "settings-changelog"
    - path: "skill/SKILL.md"
      provides: "Session Start Living State read + WHY+SEE cross-reference"
      contains: "LIVING-STATE"
  key_links:
    - from: "skill/references/stage-transitions.md"
      to: "skill/templates/living-state.md"
      via: "Transition procedure step triggers Living State regeneration"
      pattern: "LIVING-STATE"
    - from: "skill/references/do-stage.md"
      to: ".lifecycle/settings-changelog.md"
      via: "Step 5 appends settings changes"
      pattern: "settings-changelog"
    - from: "skill/SKILL.md"
      to: ".lifecycle/LIVING-STATE.md"
      via: "Session Start reads Living State"
      pattern: "LIVING-STATE"
---

<objective>
Wire memory update triggers into existing stage adapters and update SKILL.md with Living State session-start read and WHY+SEE cross-reference.

Purpose: The templates from Plan 01 define the formats. This plan makes them automatically maintained by embedding update triggers into existing workflows -- no manual user action required.
Output: Updated stage-transitions.md, do-stage.md, commit-stage.md, and SKILL.md
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-memory-decision-trail/08-CONTEXT.md
@.planning/phases/08-memory-decision-trail/08-RESEARCH.md
@.planning/phases/08-memory-decision-trail/08-01-SUMMARY.md

<interfaces>
<!-- Existing structures that will be modified -->

From skill/references/stage-transitions.md (Transition Procedure, Steps 1-5):
```markdown
## Transition Procedure

When transitioning from Stage N to Stage N+1:

1. **Read state.json** -- get current stage number and status
2. **Check gate condition** -- evaluate the gate rule
3. **If gate passes:** -- Update state.json
4. **If gate fails:** -- Set blocked, display missing
5. **Write history entry** to `.lifecycle/history/`
```
New Step 6 will be added after Step 5 for Living State update.

From skill/references/do-stage.md (Step 5: Completion):
```markdown
## Step 5: Completion

**Actions:**
a. Register outputs in manifest.json
b. Set completion timestamp
c. Update state.json
d. Display final checklist
e. Announce: "DO complete. Ready for TEST stage."
```
New step f will append memory file updates.

From skill/references/commit-stage.md (Step 4: Completion):
```markdown
## Step 4: Completion

**Actions:**
1. Register outputs in manifest.json
2. Set completion timestamp
3. Update state.json
4. Write history entry
5. Announce completion
```
New step 6 will append settings changelog update.

From skill/SKILL.md (Session Start, lines 37-51):
```markdown
## Session Start

On session start (or after compaction):

1. **Read state:** Load `.lifecycle/state.json`
2. **Display position:** ...
3. **First run:** Detect project type
4. **Check gates:** Verify manifest.json
```
Step 1.5 will be inserted to read Living State.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add memory update triggers to stage adapters</name>
  <files>skill/references/stage-transitions.md, skill/references/do-stage.md, skill/references/commit-stage.md</files>
  <read_first>
    - skill/references/stage-transitions.md (full file -- adding Step 6)
    - skill/references/do-stage.md (full file -- adding step f to Step 5)
    - skill/references/commit-stage.md (full file -- adding step 6 to Step 4)
    - .planning/phases/08-memory-decision-trail/08-RESEARCH.md (Pattern 3: Trigger-Based Updates and code examples for adapter triggers)
    - skill/templates/living-state.md (created in Plan 01 -- sections to populate)
  </read_first>
  <action>
**1. stage-transitions.md -- Add Step 6 to Transition Procedure:**

After existing Step 5 ("Write history entry"), add:

```markdown
6. **Update Living State** -- After successful transition, regenerate `.lifecycle/LIVING-STATE.md`:
   - Read current `state.json` for stage/status/feature/mode
   - Read last 20 entries from `.lifecycle/settings-changelog.md` (if exists)
   - Read last 10 entries from `.lifecycle/decisions.md` (if exists)
   - Read `.lifecycle/history/` recent entries for Event Timeline
   - Read `manifest.json` for stage completion timestamps
   - List ADRs from `docs/decisions/` (if directory exists)
   - Read `state.json` `session.resume_hint` for Resume Hint section
   - Write complete `.lifecycle/LIVING-STATE.md` using template structure from `$CLAUDE_SKILL_DIR/templates/living-state.md`
   - Target: ~100 lines max. Truncate older changelog/decision entries if needed.
```

**2. do-stage.md -- Add step f to Step 5 (Completion):**

After existing step e ("Announce"), add:

```markdown
**f. Update memory files:**

1. If any settings/config files were changed during DO (`.env*`, `*.config.*`, `package.json`, `tsconfig.json`, `*.yaml`/`*.toml` config), append entries to `.lifecycle/settings-changelog.md`:
   - Format: `| {date time} | {file path} | {what changed} | {why it changed} |`
   - If file doesn't exist yet, copy from `$CLAUDE_SKILL_DIR/templates/settings-changelog.md` first
2. If any lightweight decisions were made during DO (direction changes, small tradeoffs that don't meet ADR threshold), append to `.lifecycle/decisions.md`:
   - Format: `| {date} | {decision} | {reason} |`
   - If file doesn't exist yet, copy from `$CLAUDE_SKILL_DIR/templates/decisions.md` first
3. Regenerate `.lifecycle/LIVING-STATE.md` with current state (per stage-transitions.md Step 6 procedure)
```

**3. commit-stage.md -- Add step 6 to Step 4 (Completion):**

After existing step 5 ("Announce completion"), add:

```markdown
6. **Update memory files:**
   - If any settings/config files were committed, ensure they are recorded in `.lifecycle/settings-changelog.md`
     - Check: for each committed file matching settings patterns (`.env*`, `*.config.*`, `package.json`, `tsconfig.json`, `*.yaml`/`*.toml`), verify an entry exists in settings-changelog. If not, append one.
     - Format: `| {date time} | {file path} | {summary of change} | {from commit context} |`
   - Regenerate `.lifecycle/LIVING-STATE.md` with current state (per stage-transitions.md Step 6 procedure)
```

IMPORTANT: Do NOT modify any existing steps or content. Only APPEND the new steps at the end of the relevant sections. Preserve all existing line numbers and content.
  </action>
  <verify>
    <automated>grep "Update Living State" skill/references/stage-transitions.md && grep "LIVING-STATE" skill/references/stage-transitions.md && grep "settings-changelog" skill/references/do-stage.md && grep "decisions.md" skill/references/do-stage.md && grep "memory files" skill/references/do-stage.md && grep "settings-changelog" skill/references/commit-stage.md && grep "memory files" skill/references/commit-stage.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Update Living State" skill/references/stage-transitions.md returns match
    - grep "LIVING-STATE" skill/references/stage-transitions.md returns match
    - grep "settings-changelog" skill/references/stage-transitions.md returns match
    - grep "decisions.md" skill/references/stage-transitions.md returns match
    - grep "memory files" skill/references/do-stage.md returns match
    - grep "settings-changelog" skill/references/do-stage.md returns match
    - grep "decisions.md" skill/references/do-stage.md returns match
    - grep "memory files" skill/references/commit-stage.md returns match
    - grep "settings-changelog" skill/references/commit-stage.md returns match
    - grep "LIVING-STATE" skill/references/commit-stage.md returns match
  </acceptance_criteria>
  <done>All three stage adapters have memory update triggers appended. Existing content unchanged. Living State regeneration triggered at every stage transition, DO completion appends settings+decisions, COMMIT completion verifies settings recording.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md with Living State session-start and WHY+SEE cross-reference</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (full file -- modifying Session Start and adding Memory section)
    - .planning/phases/08-memory-decision-trail/08-CONTEXT.md (WHY+SEE already defined, MEMO-04 scope)
    - .planning/phases/08-memory-decision-trail/08-RESEARCH.md (Pattern 4: WHY+SEE Comment Chain, Open Question 3 on MEMO-04 scope)
    - skill/references/do-stage.md (Step 4: ADR Detection -- existing WHY+SEE pattern)
  </read_first>
  <action>
**1. Update Session Start section** -- Insert step 1.5 between existing step 1 ("Read state") and step 2 ("Display position"):

After "1. **Read state:** Load `.lifecycle/state.json`", add:

```markdown
1.5. **Read Living State (if exists):** Load `.lifecycle/LIVING-STATE.md`
   - This single file restores full project context: current state, active settings, recent decisions, event timeline, key ADRs, and resume hint
   - If not exists: skip (first session or pre-Phase-8 project)
   - If exists: use its contents to inform all subsequent interactions in this session
```

**2. Add Memory & Decision Trail section** to SKILL.md after the "State Management" section and before "Stage Transitions":

```markdown
## Memory & Decision Trail

Dev-lifecycle automatically maintains memory files in `.lifecycle/` to prevent context loss across sessions.

| File | Purpose | Updated When |
|------|---------|-------------|
| `.lifecycle/LIVING-STATE.md` | Session-start context snapshot (read this ONE file to restore full context) | Every stage transition + session end |
| `.lifecycle/settings-changelog.md` | Append-only record of all settings/config changes with reasons | DO completion + COMMIT completion |
| `.lifecycle/decisions.md` | Lightweight one-line decision entries (below ADR threshold) | DO completion (when decisions made) |

Templates: `$CLAUDE_SKILL_DIR/templates/living-state.md`, `settings-changelog.md`, `decisions.md`

### WHY+SEE Code Comments

When an ADR is created during DO stage, related code receives WHY+SEE comments linking implementation to the decision record:

```
// SPEC: e2e-{feature}-{NNN} -- {step title}
// WHY: {one-line decision rationale}
// SEE: docs/decisions/{NNN}-{slug}.md
```

- `// SPEC:` always comes first (structural link to spec step)
- `// WHY:` + `// SEE:` follow (decision context from ADR)
- Full workflow: `$CLAUDE_SKILL_DIR/references/do-stage.md` Step 4 (ADR Detection)
- ADR creation: Delegated to ADR skill (`~/.claude/skills/adr/SKILL.md`)

Read: `$CLAUDE_SKILL_DIR/references/do-stage.md` for full ADR detection + WHY+SEE insertion logic.
```

**3. Update the State Management table** to include the new memory files:

Add three rows to the existing State Management table:

```markdown
| `.lifecycle/LIVING-STATE.md` | Context snapshot for session restoration |
| `.lifecycle/settings-changelog.md` | Settings change history with reasons |
| `.lifecycle/decisions.md` | Lightweight decision log |
```

IMPORTANT: Keep SKILL.md under 500 lines (ARCH-01 constraint). The new section should be ~25 lines. Current file is ~262 lines so this is well within budget.
  </action>
  <verify>
    <automated>grep "Living State" skill/SKILL.md && grep "LIVING-STATE" skill/SKILL.md && grep "Memory.*Decision Trail" skill/SKILL.md && grep "WHY+SEE" skill/SKILL.md && grep "settings-changelog" skill/SKILL.md && wc -l < skill/SKILL.md | xargs test 500 -ge && echo "OK: under 500 lines" || echo "FAIL: over 500 lines"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Living State" skill/SKILL.md returns match (session start step)
    - grep "LIVING-STATE" skill/SKILL.md returns match (file reference)
    - grep "Memory" skill/SKILL.md returns match (new section header)
    - grep "WHY+SEE" skill/SKILL.md returns match (MEMO-04 cross-reference)
    - grep "settings-changelog" skill/SKILL.md returns match (memory file reference)
    - grep "decisions.md" skill/SKILL.md returns match (memory file reference)
    - grep "do-stage.md" skill/SKILL.md returns match for WHY+SEE reference
    - wc -l skill/SKILL.md shows under 500 lines
  </acceptance_criteria>
  <done>SKILL.md has Living State read at session start (step 1.5), Memory & Decision Trail section with WHY+SEE cross-reference (MEMO-04), state management table includes new files, total under 500 lines</done>
</task>

</tasks>

<verification>
All four MEMO requirements are addressed:
- MEMO-01: settings-changelog triggers in do-stage.md and commit-stage.md
- MEMO-02: decisions.md trigger in do-stage.md
- MEMO-03: Living State regeneration in stage-transitions.md + session-start read in SKILL.md
- MEMO-04: WHY+SEE cross-reference section in SKILL.md pointing to do-stage.md Step 4

Stage adapters have memory update triggers:
- `grep "memory files" skill/references/do-stage.md` returns match
- `grep "memory files" skill/references/commit-stage.md` returns match
- `grep "Update Living State" skill/references/stage-transitions.md` returns match
- `grep "LIVING-STATE" skill/SKILL.md` returns match
</verification>

<success_criteria>
- stage-transitions.md has Step 6 for Living State regeneration on every transition
- do-stage.md Step 5 has step f for settings + decisions + Living State updates
- commit-stage.md Step 4 has step 6 for settings verification + Living State update
- SKILL.md Session Start reads Living State (step 1.5)
- SKILL.md has Memory & Decision Trail section with WHY+SEE cross-reference
- SKILL.md State Management table lists new memory files
- SKILL.md remains under 500 lines
- No existing adapter content was modified (only appended)
</success_criteria>

<output>
After completion, create `.planning/phases/08-memory-decision-trail/08-02-SUMMARY.md`
</output>
