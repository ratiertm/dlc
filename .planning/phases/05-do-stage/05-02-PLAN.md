---
phase: 05-do-stage
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - skill/SKILL.md
  - skill/references/role-matrix.md
  - ~/.claude/skills/dev-lifecycle/SKILL.md
  - ~/.claude/skills/dev-lifecycle/references/role-matrix.md
autonomous: true
requirements: [PIPE-02]

must_haves:
  truths:
    - "SKILL.md Stage 2 section lists dev-lifecycle as primary skill with Read directive to do-stage.md"
    - "SKILL.md Stage 2 section describes the 5-step pipeline (init, spec load, implementation, deviation, ADR, completion)"
    - "role-matrix.md Stage 2 row shows dev-lifecycle as primary, PDCA as supporting"
    - "Runtime copies match version-controlled files"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Updated Stage 2 section"
      contains: "do-stage.md"
    - path: "skill/references/role-matrix.md"
      provides: "Updated Stage 2 row"
      contains: "dev-lifecycle"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/do-stage.md"
      via: "Read directive ($CLAUDE_SKILL_DIR/references/do-stage.md)"
      pattern: "do-stage\\.md"
    - from: "skill/references/role-matrix.md"
      to: "skill/SKILL.md"
      via: "Stage 2 primary skill alignment"
      pattern: "Stage.*2.*DO"
---

<objective>
Update SKILL.md and role-matrix.md to reflect dev-lifecycle as primary skill for Stage 2 (DO), following the exact same pattern established in Phase 4 for Stage 1 (PLAN).

Purpose: Aligns skill documentation with the new do-stage.md adapter. Without this update, SKILL.md still lists PDCA as primary for Stage 2, which contradicts the Phase 4 decision pattern where dev-lifecycle became primary for inner loop stages.

Output: Updated SKILL.md Stage 2 section + updated role-matrix.md Stage 2 row + runtime copies
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-do-stage/05-CONTEXT.md
@.planning/phases/04-plan-stage/04-02-SUMMARY.md

<interfaces>
<!-- Current SKILL.md Stage 2 section (lines 70-81) to be replaced -->
From skill/SKILL.md (Stage 2: DO, current):
```markdown
### Stage 2: DO

Purpose: Implement the plan. Track progress and decisions.
Primary skill: PDCA (`/pdca do`)
Supporting: ADR (auto-detect trade-offs), WHY+SEE code comments
Outputs: Code changes, ADR documents, progress updates

During implementation:
- Detect trade-offs ("chose A over B") and suggest ADR creation
- Insert WHY+SEE comments at decision points in code
- Flag large direction changes for PLAN update
```

<!-- Current role-matrix.md Stage 2 row to be updated -->
From skill/references/role-matrix.md (Stage 2 row):
```
| 2 | DO | PDCA (`/pdca do`) | ADR (auto-detect decisions), WHY+SEE comments | Track implementation progress, update state.json |
```

<!-- Phase 4 pattern for Stage 1 update (04-02-SUMMARY.md) -->
Pattern: SKILL.md Stage 1 now has `Read: $CLAUDE_SKILL_DIR/references/plan-stage.md` and describes 4-step pipeline.
Pattern: role-matrix.md Stage 1 shows `dev-lifecycle (spec + prototype generation)` as primary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md Stage 2 section and role-matrix.md Stage 2 row</name>
  <files>skill/SKILL.md, skill/references/role-matrix.md</files>
  <read_first>
    - skill/SKILL.md (full file -- find Stage 2 section boundaries, preserve everything else)
    - skill/references/role-matrix.md (full file -- find Stage 2 row, preserve everything else)
    - skill/references/do-stage.md (just created in Plan 01 -- confirm it exists and get the step names for pipeline description)
  </read_first>
  <action>
**SKILL.md Stage 2 section** -- Replace lines 70-81 (the current `### Stage 2: DO` section) with:

```markdown
### Stage 2: DO

Purpose: Implement against approved spec. Track progress, log deviations, detect ADR-worthy decisions.
Primary: dev-lifecycle (spec-driven implementation tracking)
Supporting: ADR (auto-detect trade-offs), WHY+SEE code comments, PDCA (quality support)
Outputs: Code changes, updated spec (step statuses + deviations), ADR documents

Read: `$CLAUDE_SKILL_DIR/references/do-stage.md`

Pipeline:
1. Stage init (gate check, spec load, status update)
2. Spec load and checklist build (read spec from disk, display progress)
3. Per-step implementation (implement each chain step, add SPEC comments)
4. Deviation handling (detect, present to user, record with approval)
5. ADR detection (deviation + tradeoff triggers, suggest ADR creation)
6. Completion (artifact registration, state update)
```

Preserve ALL other content in SKILL.md. Only modify the Stage 2 section.

**role-matrix.md Stage 2 row** -- Replace the Stage 2 row in the table:

From:
```
| 2 | DO | PDCA (`/pdca do`) | ADR (auto-detect decisions), WHY+SEE comments | Track implementation progress, update state.json |
```

To:
```
| 2 | DO | dev-lifecycle (spec-driven implementation) | PDCA (quality support), ADR (auto-detect decisions), WHY+SEE comments | Run spec checklist, track per-step status, log deviations, detect ADR moments |
```

Also update the Skill Overlap Resolution table in role-matrix.md. Add or update a row for Stage 2:
```
| dev-lifecycle vs PDCA in Stage 2 | dev-lifecycle is primary for spec-driven implementation tracking. PDCA is available for quality cycles if user invokes directly. These are complementary. |
```

And update Stage Output Types table row for Stage 2:
```
| 2 DO | code, spec-updated (required), adr (optional) |
```

Preserve ALL other content in role-matrix.md. Only modify the Stage 2 specific entries.
  </action>
  <verify>
    <automated>grep "dev-lifecycle.*spec-driven" skill/SKILL.md && grep "do-stage.md" skill/SKILL.md && grep "dev-lifecycle.*spec-driven" skill/references/role-matrix.md && echo "ALL PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Primary: dev-lifecycle" skill/SKILL.md returns match in Stage 2 section
    - grep "do-stage.md" skill/SKILL.md returns match (Read directive)
    - grep "Pipeline:" skill/SKILL.md followed by 6 numbered items in Stage 2 section
    - grep "dev-lifecycle.*spec-driven" skill/references/role-matrix.md returns match in Stage 2 row
    - grep "spec-updated" skill/references/role-matrix.md returns match in Stage 2 output types
    - Stage 1 section in SKILL.md is unchanged (grep "plan-stage.md" still matches)
    - Stage 3+ sections in SKILL.md are unchanged
  </acceptance_criteria>
  <done>SKILL.md Stage 2 shows dev-lifecycle as primary with Read directive to do-stage.md. role-matrix.md Stage 2 row shows dev-lifecycle as primary with updated outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy updated files to runtime skill directory</name>
  <files>~/.claude/skills/dev-lifecycle/SKILL.md, ~/.claude/skills/dev-lifecycle/references/role-matrix.md</files>
  <read_first>
    - skill/SKILL.md (source -- just updated in Task 1)
    - skill/references/role-matrix.md (source -- just updated in Task 1)
  </read_first>
  <action>
Copy the updated files to their runtime locations:
1. `cp skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md`
2. `cp skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md`

This follows the dual-location pattern from Phase 1 decision [01-01].
  </action>
  <verify>
    <automated>diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md && diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md && echo "ALL MATCH"</automated>
  </verify>
  <acceptance_criteria>
    - diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md shows no differences
    - diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md shows no differences
  </acceptance_criteria>
  <done>Runtime copies at ~/.claude/skills/dev-lifecycle/ are identical to version-controlled sources</done>
</task>

</tasks>

<verification>
- `grep "do-stage.md" skill/SKILL.md` -- Read directive present
- `grep "dev-lifecycle" skill/SKILL.md | grep -i "stage 2\|DO"` -- dev-lifecycle is primary
- `grep "dev-lifecycle" skill/references/role-matrix.md | grep "DO"` -- role-matrix aligned
- `diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md` -- runtime matches
- `diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md` -- runtime matches
</verification>

<success_criteria>
- SKILL.md Stage 2 section references do-stage.md via Read directive
- SKILL.md Stage 2 shows dev-lifecycle as primary, PDCA as supporting
- role-matrix.md Stage 2 row shows dev-lifecycle as primary
- All runtime copies match version-controlled files
- No other sections in SKILL.md or role-matrix.md are modified
</success_criteria>

<output>
After completion, create `.planning/phases/05-do-stage/05-02-SUMMARY.md`
</output>
