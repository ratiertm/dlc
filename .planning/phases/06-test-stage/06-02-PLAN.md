---
phase: 06-test-stage
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - skill/SKILL.md
  - skill/references/role-matrix.md
  - ~/.claude/skills/dev-lifecycle/SKILL.md
  - ~/.claude/skills/dev-lifecycle/references/role-matrix.md
autonomous: true
requirements: [PIPE-03]

must_haves:
  truths:
    - "SKILL.md Stage 3 section shows dev-lifecycle as primary with Read directive to test-stage.md"
    - "SKILL.md Stage 3 pipeline lists all verification steps (spec, prototype, behavioral, dual gate)"
    - "role-matrix.md Stage 3 row shows dev-lifecycle as primary instead of GSD"
    - "role-matrix.md overlap resolution includes dev-lifecycle vs GSD for Stage 3"
    - "role-matrix.md output types shows verification and test-report for Stage 3"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Updated Stage 3 section"
      contains: "Read: $CLAUDE_SKILL_DIR/references/test-stage.md"
    - path: "skill/references/role-matrix.md"
      provides: "Updated Stage 3 role mapping"
      contains: "dev-lifecycle"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/test-stage.md"
      via: "Read directive in Stage 3 section"
      pattern: "test-stage\\.md"
    - from: "skill/references/role-matrix.md"
      to: "skill/SKILL.md"
      via: "Stage 3 primary skill alignment"
      pattern: "Stage.*3.*dev-lifecycle"
---

<objective>
Update SKILL.md Stage 3 section and role-matrix.md to reflect dev-lifecycle as primary skill for TEST stage, with Read directive pointing to test-stage.md.

Purpose: Align skill documentation so Claude reads test-stage.md when entering Stage 3, matching the pattern established for Stage 1 (plan-stage.md) and Stage 2 (do-stage.md).
Output: Updated SKILL.md and role-matrix.md with Stage 3 alignment.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-test-stage/06-CONTEXT.md
@.planning/phases/05-do-stage/05-02-SUMMARY.md

@skill/SKILL.md
@skill/references/role-matrix.md

<interfaces>
<!-- From 05-02-SUMMARY.md: established pattern for stage skill doc updates -->
Pattern: "Stage primary skill update: replace old primary, add Read directive, describe pipeline steps, update all 3 role-matrix tables"

Current SKILL.md Stage 3 (lines 87-100):
  Purpose: Verify implementation against plan criteria.
  Primary skill: GSD (`/gsd:verify-work`)  <-- MUST CHANGE to dev-lifecycle
  Supporting: PDCA (`/pdca analyze`)
  Outputs: Verification report, gap analysis

Current role-matrix.md Stage 3 row:
  | 3 | TEST | GSD (`/gsd:verify-work`) | PDCA (`/pdca analyze`) | Run verification, compare against manifest, update status |
  <-- MUST CHANGE primary to dev-lifecycle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md Stage 3 section and role-matrix.md Stage 3 row</name>
  <files>skill/SKILL.md, skill/references/role-matrix.md</files>
  <read_first>
    - skill/SKILL.md (full file -- find Stage 3 section to replace, reference Stage 2 section as pattern)
    - skill/references/role-matrix.md (full file -- find Stage 3 rows in all 3 tables)
    - skill/references/test-stage.md (created in Plan 01 -- verify it exists and get exact filename for Read directive)
  </read_first>
  <action>
**SKILL.md Stage 3 update:**

Replace the current Stage 3 section (lines ~87-100) with the dev-lifecycle primary pattern matching Stage 2's format:

```markdown
### Stage 3: TEST

Purpose: Verify implementation against E2E spec (per-step pass/fail), compare prototype manifest structure against code, generate user behavioral verification checklist. Dual gate: both Claude structural and user behavioral verification required before COMMIT.
Primary: dev-lifecycle (spec + prototype verification)
Supporting: GSD (`/gsd:verify-work`), PDCA (`/pdca analyze`)
Outputs: Verification report (verification.json + verification.md), user behavioral checklist

Read: `$CLAUDE_SKILL_DIR/references/test-stage.md`

Pipeline:
1. Stage init (gate check, spec + prototype load, deviation scan)
2. Spec step enumeration (parse all steps, display verification plan)
3. Per-step spec verification (check code against each step's criteria)
4. Prototype structural comparison (manifest vs implementation)
5. User behavioral checklist generation (spec steps -> user actions)
6. Completion (generate reports, dual gate enforcement)
```

Keep Stage 3 heading level and surrounding sections intact.

**role-matrix.md Stage 3 updates (all 3 tables):**

1. **Stage-Skill Mapping table:** Change Stage 3 row:
   - Primary: `dev-lifecycle (spec + prototype verification)` (was `GSD`)
   - Supporting: `GSD (/gsd:verify-work), PDCA (/pdca analyze)` (GSD moves to supporting)
   - dev-lifecycle Role: `Run per-step spec verification, prototype structural comparison, generate behavioral checklist, enforce dual verification gate`

2. **Skill Overlap Resolution table:** Add or update row for `dev-lifecycle vs GSD in Stage 3`:
   - Resolution: `dev-lifecycle is primary for spec-driven and prototype structural verification. GSD is available for broader project-level verification. PDCA provides gap analysis. These are complementary.`

3. **Stage Output Types table:** Update Stage 3 row:
   - Expected Output Types: `verification (required), test-report (required)` (was just "verification, test-report" -- add "(required)" for clarity matching Stage 2 pattern)
  </action>
  <verify>
    <automated>grep -q "test-stage.md" skill/SKILL.md && grep -q "dev-lifecycle" skill/references/role-matrix.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Read.*test-stage.md" skill/SKILL.md (Read directive exists)
    - grep -q "Primary.*dev-lifecycle\|dev-lifecycle.*spec.*prototype" skill/SKILL.md (dev-lifecycle is primary)
    - grep "3.*TEST" skill/references/role-matrix.md shows dev-lifecycle as primary (not GSD)
    - grep -q "dev-lifecycle vs GSD.*Stage 3\|Stage 3.*dev-lifecycle.*GSD" skill/references/role-matrix.md (overlap resolution updated)
    - grep -q "verification.*required\|test-report" skill/references/role-matrix.md (output types updated)
  </acceptance_criteria>
  <done>SKILL.md Stage 3 section shows dev-lifecycle as primary with Read directive to test-stage.md and 6-step pipeline. role-matrix.md updated across all 3 tables (mapping, overlap, output types) consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy updated files to runtime skill directory</name>
  <files>~/.claude/skills/dev-lifecycle/SKILL.md, ~/.claude/skills/dev-lifecycle/references/role-matrix.md</files>
  <read_first>
    - skill/SKILL.md (version-controlled source just updated in Task 1)
    - skill/references/role-matrix.md (version-controlled source just updated in Task 1)
  </read_first>
  <action>
Copy updated files to runtime skill directory:

```bash
cp skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md
cp skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md
```

Verify both copies match their sources with `diff`.
  </action>
  <verify>
    <automated>diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md && diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md && echo "ALL MATCH"</automated>
  </verify>
  <acceptance_criteria>
    - diff skill/SKILL.md ~/.claude/skills/dev-lifecycle/SKILL.md shows no differences
    - diff skill/references/role-matrix.md ~/.claude/skills/dev-lifecycle/references/role-matrix.md shows no differences
  </acceptance_criteria>
  <done>Runtime copies at ~/.claude/skills/dev-lifecycle/ match version-controlled sources exactly for both SKILL.md and role-matrix.md.</done>
</task>

</tasks>

<verification>
- SKILL.md Stage 3 section has dev-lifecycle as primary with Read directive to test-stage.md
- role-matrix.md has dev-lifecycle as primary for Stage 3 across all 3 tables
- Runtime copies match version-controlled sources
- Pattern matches Stage 2 update from Phase 05-02
</verification>

<success_criteria>
- SKILL.md Stage 3 mirrors Stage 2 format: Primary, Supporting, Outputs, Read directive, Pipeline steps
- role-matrix.md consistently reflects dev-lifecycle as Stage 3 primary across mapping, overlap resolution, and output types tables
- Runtime and version-controlled files are identical
</success_criteria>

<output>
After completion, create `.planning/phases/06-test-stage/06-02-SUMMARY.md`
</output>
