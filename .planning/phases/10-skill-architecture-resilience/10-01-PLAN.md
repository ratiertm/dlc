---
phase: 10-skill-architecture-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/settings.json
  - skill/references/state-reconcile.md
  - skill/references/stage-transitions.md
  - skill/SKILL.md
autonomous: true
requirements: [ARCH-03, ARCH-05, FOUND-04]

must_haves:
  truths:
    - "SessionStart hook automatically loads LIVING-STATE.md and state.json into Claude context on every session start"
    - "If state.json is missing but .lifecycle/ exists, reconcile procedure can reconstruct state from filesystem artifacts"
    - "Execution mode (hotfix/feature/release/milestone) causes non-required stages to be auto-skipped during transitions"
  artifacts:
    - path: ".claude/settings.json"
      provides: "SessionStart hook configuration"
      contains: "SessionStart"
    - path: "skill/references/state-reconcile.md"
      provides: "Reconcile algorithm documentation"
      contains: "Reconcile"
    - path: "skill/references/stage-transitions.md"
      provides: "Auto-skip procedure for mode-based stage skipping"
      contains: "auto-skip"
    - path: "skill/SKILL.md"
      provides: "Reconcile check in Session Start, mode display"
      contains: "reconcile"
  key_links:
    - from: ".claude/settings.json"
      to: ".lifecycle/LIVING-STATE.md"
      via: "cat command in SessionStart hook"
      pattern: "cat .lifecycle/LIVING-STATE.md"
    - from: "skill/SKILL.md"
      to: "skill/references/state-reconcile.md"
      via: "Read directive in Session Start step 1"
      pattern: "references/state-reconcile.md"
    - from: "skill/references/stage-transitions.md"
      to: "state.json current.mode"
      via: "Auto-skip procedure checks mode before transitioning"
      pattern: "auto-skip"
---

<objective>
Create SessionStart hook for automatic context loading, state reconcile procedure for resilience, and wire execution mode skip logic into stage transitions.

Purpose: Make the skill production-ready with automatic session context, state recovery, and mode-based stage skipping.
Output: SessionStart hook config, reconcile reference, updated stage-transitions with auto-skip, SKILL.md session start updates.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-skill-architecture-resilience/10-CONTEXT.md
@.planning/phases/10-skill-architecture-resilience/10-RESEARCH.md

<interfaces>
<!-- Key structures the executor needs -->

From skill/SKILL.md Session Start (lines 37-55):
```markdown
## Session Start

On session start (or after compaction):

1. **Read state:** Load `.lifecycle/state.json`
   - If not exists: initialize from `$CLAUDE_SKILL_DIR/templates/state.json`, create `.lifecycle/` directory
1.5. **Read Living State (if exists):** Load `.lifecycle/LIVING-STATE.md`
   ...
2. **Display position:**
   ...
```

From skill/templates/state.json:
```json
{
  "version": "1.0",
  "current": {
    "stage": 1,
    "stage_name": "PLAN",
    "status": "not_started",
    "feature": null,
    "mode": "feature"
  },
  "progress": {
    "stages_completed": [],
    "current_stage_started_at": null,
    "last_transition_at": null
  },
  "session": {
    "last_active": null,
    "resume_hint": ""
  }
}
```

From skill/references/stage-transitions.md Mode-Based Stage Skipping table:
```
| Mode      | Skippable Stages                              |
|-----------|-----------------------------------------------|
| hotfix    | 1 (PLAN), 7 (DOCUMENT), 8 (RETROSPECT), 9    |
| feature   | 5 (DEPLOY), 6 (DEPLOY TEST), 9 (PROMOTE)     |
| release   | 9 (PROMOTE)                                   |
| milestone | None                                          |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionStart hook and state-reconcile reference</name>
  <read_first>
    - skill/SKILL.md (lines 37-55, Session Start section)
    - skill/references/stage-transitions.md (full file)
    - skill/templates/state.json
    - skill/templates/manifest.json
    - .claude/settings.local.json
    - .planning/phases/10-skill-architecture-resilience/10-RESEARCH.md (Architecture Patterns sections)
  </read_first>
  <files>.claude/settings.json, skill/references/state-reconcile.md</files>
  <action>
    **File 1: `.claude/settings.json`**

    Create project-scoped settings file (NOT settings.local.json which has permissions). Content:

    ```json
    {
      "hooks": {
        "SessionStart": [
          {
            "matcher": "",
            "hooks": [
              {
                "type": "command",
                "command": "cat .lifecycle/LIVING-STATE.md 2>/dev/null && echo '---' && cat .lifecycle/state.json 2>/dev/null || echo 'No lifecycle state found. Run dev-lifecycle to initialize.'",
                "timeout": 5,
                "statusMessage": "Loading dev-lifecycle context..."
              }
            ]
          }
        ]
      }
    }
    ```

    Key: Empty string matcher `""` matches ALL session types (startup, resume, clear, compact). stdout is automatically injected as Claude context.

    **File 2: `skill/references/state-reconcile.md`**

    Create reconcile procedure reference file. Must include:

    1. Title: "# State Reconcile Procedure"
    2. Purpose: recover state.json from .lifecycle/ filesystem when lost/corrupted
    3. When triggered: state.json missing but .lifecycle/ directory exists
    4. Algorithm (6 steps from RESEARCH.md Pattern 2):
       - Step 1: Check manifest.json existence. If missing -> fresh init from templates.
       - Step 2: Scan manifest.json artifacts from stage 9 down to 1. Find highest completed stage (outputs.length > 0 AND completed_at != null).
       - Step 3: Determine current stage = last_completed + 1. If last_completed is 9: reset to 1. If none: stage 1.
       - Step 4: Infer mode from skip patterns in .lifecycle/history/. Match to known mode patterns (hotfix: 1,7,8,9 skipped; feature: 5,6,9 skipped; release: 9 skipped; milestone: nothing skipped). Default: "feature".
       - Step 5: Reconstruct state.json with conservative defaults: status "not_started", resume_hint "State recovered via reconcile. Verify current position."
       - Step 6: Write state.json, log reconcile event to .lifecycle/history/
    5. Post-reconcile: Display result to user for confirmation. User can correct if wrong.
    6. Edge cases: empty manifest (fresh init), partial history, no history/ directory
  </action>
  <verify>
    <automated>cat .claude/settings.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert 'SessionStart' in d['hooks']; assert d['hooks']['SessionStart'][0]['hooks'][0]['type'] == 'command'; print('OK')" && test -f skill/references/state-reconcile.md && grep -q "Reconcile" skill/references/state-reconcile.md && grep -q "manifest.json" skill/references/state-reconcile.md && echo "PASS"</automated>
  </verify>
  <done>
    - .claude/settings.json exists with valid SessionStart hook that cats LIVING-STATE.md and state.json
    - skill/references/state-reconcile.md exists with 6-step reconcile algorithm
    - settings.json does NOT conflict with settings.local.json (separate files)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-skip logic and update SKILL.md Session Start</name>
  <read_first>
    - skill/SKILL.md (full file)
    - skill/references/stage-transitions.md (full file, especially Transition Procedure section)
    - skill/references/state-reconcile.md (just created in Task 1)
  </read_first>
  <files>skill/references/stage-transitions.md, skill/SKILL.md</files>
  <action>
    **File 1: `skill/references/stage-transitions.md`**

    Add a new section "## Auto-Skip Procedure" after the existing "Mode-Based Stage Skipping" section. Content:

    ```markdown
    ## Auto-Skip Procedure

    When transitioning from completed Stage N, apply mode-based auto-skip:

    1. Determine next_stage = N + 1
    2. Read current mode from state.json (`current.mode`)
    3. Look up skippable stages for current mode (see Mode-Based Stage Skipping table)
    4. If next_stage is in the skippable list:
       a. Set stage next_stage status to "skipped" in state.json
       b. Record skip in .lifecycle/history/ with entry:
          ```json
          {
            "timestamp": "ISO-8601",
            "from_stage": N,
            "to_stage": next_stage,
            "gate_result": "skipped",
            "reason": "Stage skipped by {mode} mode",
            "mode": "{mode}"
          }
          ```
       c. Increment: N = next_stage, repeat from step 1
    5. If next_stage is NOT skippable: transition normally (continue with standard Transition Procedure)

    Note: Auto-skip cascades -- if stages 5 and 6 are both skippable, transitioning from 4 skips to 7 in one pass.
    ```

    Also update the existing "Transition Procedure" section step 3 to reference auto-skip:
    Add after step 3's bullet points: "3.5. **Check auto-skip:** If next stage is skippable in current mode, run Auto-Skip Procedure instead of entering next stage."

    **File 2: `skill/SKILL.md`**

    Update Session Start section (lines 37-55). Replace the current steps with:

    ```markdown
    ## Session Start

    On session start (or after compaction):

    1. **Read state:** Load `.lifecycle/state.json`
       - If not exists AND `.lifecycle/` directory exists: **Reconcile** (Read: `$CLAUDE_SKILL_DIR/references/state-reconcile.md`)
       - If not exists AND no `.lifecycle/`: Initialize from `$CLAUDE_SKILL_DIR/templates/state.json`, create `.lifecycle/` directory
    2. **Read Living State (if exists):** Load `.lifecycle/LIVING-STATE.md`
       - This single file restores full project context: current state, active settings, recent decisions, event timeline, key ADRs, and resume hint
       - If not exists: skip (first session or pre-Phase-8 project)
       - If exists: use its contents to inform all subsequent interactions in this session
       - Note: This is also loaded automatically by SessionStart hook (.claude/settings.json)
    3. **Display position:**
       ```
       Dev Lifecycle: Stage {N} {NAME} -- {STATUS}
       Feature: {feature_name}
       Mode: {mode} (skipping stages: {skippable_list or "none"})
       Resume: {resume_hint}
       ```
    4. **First run:** Detect project type
       - Read: `$CLAUDE_SKILL_DIR/references/project-detection.md`
       - Auto-detect from marker files, present to user for confirmation
       - Store confirmed type in `state.json`
    5. **Check gates:** Verify manifest.json for current stage requirements
    ```

    Key changes from current:
    - Step 1 adds reconcile branch (Read directive to state-reconcile.md)
    - Step 1.5 renumbered to Step 2 (clean numbering)
    - Step 2 (old) becomes Step 3, adds Mode display line with skippable stages
    - Steps 3-4 (old) become Steps 4-5
    - Note added about SessionStart hook double-loading (harmless, provides redundancy)

    IMPORTANT: Keep SKILL.md under 500 lines total. The changes are net-neutral (replacing steps, not adding sections).
  </action>
  <verify>
    <automated>grep -q "auto-skip\|Auto-Skip" skill/references/stage-transitions.md && grep -q "reconcile\|Reconcile" skill/SKILL.md && grep -q "state-reconcile.md" skill/SKILL.md && grep -q "Mode:" skill/SKILL.md && wc -l skill/SKILL.md | awk '{print ($1 <= 500) ? "PASS: " $1 " lines" : "FAIL: " $1 " lines"}'</automated>
  </verify>
  <done>
    - stage-transitions.md has Auto-Skip Procedure section with cascading skip logic
    - Transition Procedure references auto-skip at step 3.5
    - SKILL.md Session Start has reconcile branch in step 1 with Read directive to state-reconcile.md
    - SKILL.md Session Start displays execution mode and skippable stages
    - SKILL.md remains under 500 lines
  </done>
</task>

</tasks>

<verification>
- `.claude/settings.json` is valid JSON with SessionStart hook
- `skill/references/state-reconcile.md` exists with complete reconcile algorithm
- `skill/references/stage-transitions.md` has Auto-Skip Procedure
- `skill/SKILL.md` Session Start includes reconcile, mode display, and is <= 500 lines
- No changes to `.claude/settings.local.json` (permissions file untouched)
</verification>

<success_criteria>
- SessionStart hook will auto-load LIVING-STATE.md and state.json on every session start (ARCH-03)
- State reconcile procedure documented and wired into SKILL.md (ARCH-05)
- Execution mode skip logic wired into stage transitions (FOUND-04)
- SKILL.md under 500 lines
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-architecture-resilience/10-01-SUMMARY.md`
</output>
