---
phase: 10-skill-architecture-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/SKILL.md
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "SKILL.md is under 500 lines with all detailed logic in references/ files"
    - "All 9 stages use Read directives to adapter files, no inline stage logic in SKILL.md"
    - "Existing skills (GSD, PDCA, ADR, retro, work-log) are referenced via adapter interfaces without modification"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Progressive disclosure hub under 500 lines"
      min_lines: 200
    - path: "skill/references/plan-stage.md"
      provides: "Stage 1 adapter"
    - path: "skill/references/do-stage.md"
      provides: "Stage 2 adapter"
    - path: "skill/references/test-stage.md"
      provides: "Stage 3 adapter"
    - path: "skill/references/commit-stage.md"
      provides: "Stage 4 adapter"
    - path: "skill/references/deploy-stage.md"
      provides: "Stage 5 adapter"
    - path: "skill/references/deploy-test-stage.md"
      provides: "Stage 6 adapter"
    - path: "skill/references/document-stage.md"
      provides: "Stage 7 adapter"
    - path: "skill/references/retrospect-stage.md"
      provides: "Stage 8 adapter"
    - path: "skill/references/promote-stage.md"
      provides: "Stage 9 adapter"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/*-stage.md"
      via: "Read: $CLAUDE_SKILL_DIR/references/ directives"
      pattern: "Read:.*references/"
    - from: "skill/references/role-matrix.md"
      to: "GSD, PDCA, ADR, retro, work-log skills"
      via: "Supporting skill references without modification"
      pattern: "Supporting:"
---

<objective>
Verify and audit ARCH-01 (progressive disclosure, 500-line limit) and ARCH-02 (adapter pattern, no external skill modification) across the entire skill.

Purpose: Confirm production-readiness of the skill architecture -- all patterns established in Phases 1-9 are consistent and complete.
Output: Verified SKILL.md with audit confirmation, any minor cleanup applied.
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-skill-architecture-resilience/10-CONTEXT.md

<interfaces>
<!-- Current SKILL.md structure (from Phase 9 completion) -->
SKILL.md sections:
- Frontmatter (name, description)
- The 9-Stage Pipeline (ASCII diagram)
- Session Start (steps 1-4)
- Stage 1-9 Overview (each with Purpose/Primary/Supporting/Outputs/Read/Pipeline)
- State Management (table of .lifecycle/ files)
- Memory & Decision Trail (tables + WHY+SEE)
- Stage Transitions (Read directive)
- Execution Modes (mode table)
- Skill Orchestration (orchestration pattern + Read directives)
- Project Type Detection (Read directive)
- Phase Transition Detection (signal table)
- Edge Cases
- Output Language

references/ directory (15 files):
- 9 stage adapters: plan-stage.md, do-stage.md, test-stage.md, commit-stage.md, deploy-stage.md, deploy-test-stage.md, document-stage.md, retrospect-stage.md, promote-stage.md
- 6 supporting: e2e-spec.md, prototype.md, project-detection.md, skill-invocation.md, role-matrix.md, stage-transitions.md

templates/ directory (7 files):
- state.json, manifest.json, spec-template.md, prototype-template.html, living-state.md, settings-changelog.md, decisions.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit progressive disclosure and adapter pattern compliance</name>
  <read_first>
    - skill/SKILL.md (full file)
    - skill/references/role-matrix.md
    - skill/references/skill-invocation.md
  </read_first>
  <files>skill/SKILL.md</files>
  <action>
    **Audit ARCH-01 (Progressive Disclosure):**

    1. Run `wc -l skill/SKILL.md` -- must be <= 500 lines
    2. Verify every stage section (1-9) has a `Read:` directive pointing to its adapter file
    3. Verify no stage section contains inline implementation logic (more than 5 lines of procedure). Pipeline summaries (numbered steps, 1 line each) are OK. Detailed multi-line procedures must be in references/.
    4. Verify references/ has all 15 expected files (9 stage adapters + 6 supporting)
    5. Verify templates/ has all 7 expected files
    6. Document audit results as a comment block at the bottom of SKILL.md:

    ```markdown
    <!-- Architecture Audit (Phase 10)
    ARCH-01: SKILL.md {N} lines (limit: 500) -- PASS
    ARCH-02: All 9 stages use Read directives, no inline logic -- PASS
    References: {N}/15 files present -- PASS
    Templates: {N}/7 files present -- PASS
    Audit date: 2026-03-22
    -->
    ```

    **Audit ARCH-02 (Adapter Pattern):**

    1. Read role-matrix.md -- verify all 9 stages list dev-lifecycle as Primary
    2. Verify Supporting skills are referenced by name only (no modification instructions, no direct code changes to external skills)
    3. Read skill-invocation.md -- verify invocation commands reference existing skill entry points (e.g., `/gsd:verify-work`, `/pdca plan`) without assuming modifications
    4. Check that SKILL.md's "Skill Orchestration" section states the orchestration-only pattern: "dev-lifecycle orchestrates existing skills. It never executes stage work directly."

    **If any issue found:** Fix it in-place. Expected issues: none (all patterns were established correctly in Phases 1-9). This is a verification pass.

    **If SKILL.md needs minor cleanup** (redundant text, inconsistent formatting between stages, outdated references): Apply fixes. Keep changes minimal -- this is hardening, not restructuring.
  </action>
  <verify>
    <automated>wc -l skill/SKILL.md | awk '{print ($1 <= 500) ? "PASS: " $1 " lines" : "FAIL: " $1 " lines"}' && ls skill/references/*-stage.md | wc -l | awk '{print ($1 == 9) ? "PASS: 9 adapters" : "FAIL: " $1 " adapters"}' && ls skill/references/*.md | wc -l | awk '{print ($1 >= 15) ? "PASS: " $1 " refs" : "FAIL: " $1 " refs"}' && ls skill/templates/ | wc -l | awk '{print ($1 >= 7) ? "PASS: " $1 " templates" : "FAIL: " $1 " templates"}' && grep -c "Read:.*references/" skill/SKILL.md | awk '{print ($1 >= 9) ? "PASS: " $1 " Read directives" : "FAIL: " $1 " Read directives"}'</automated>
  </verify>
  <done>
    - SKILL.md is verified under 500 lines
    - All 9 stages have Read directives to adapter files
    - All 15 reference files and 7 template files exist
    - No inline stage logic in SKILL.md (only pipeline summaries)
    - role-matrix.md confirms dev-lifecycle as primary for all stages
    - Supporting skills referenced without modification
    - Audit comment block added to SKILL.md bottom
  </done>
</task>

</tasks>

<verification>
- `wc -l skill/SKILL.md` outputs <= 500
- `ls skill/references/*-stage.md | wc -l` outputs 9
- `grep -c "Read:.*references/" skill/SKILL.md` outputs >= 9
- `grep "Architecture Audit" skill/SKILL.md` finds audit block
</verification>

<success_criteria>
- ARCH-01 verified: SKILL.md under 500 lines, all logic in references/ (progressive disclosure complete)
- ARCH-02 verified: All stages use adapter pattern, existing skills called without modification
- Audit trail embedded in SKILL.md as HTML comment
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-architecture-resilience/10-02-SUMMARY.md`
</output>
