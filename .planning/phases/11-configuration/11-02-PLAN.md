---
phase: 11-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - skill/SKILL.md
  - skill/references/stage-transitions.md
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "SKILL.md has a Configuration section with lifecycle-config commands in phase transition detection"
    - "Stage transitions resolve mode from config layers (env > config.yaml > state.json default) instead of only state.json"
    - "SKILL.md remains under 500 lines after additions"
    - "settings-changelog.md rules include lifecycle config files"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Configuration entry point and phase transition detection for config commands"
      contains: "lifecycle-config"
    - path: "skill/references/stage-transitions.md"
      provides: "Config-aware mode resolution in transition procedure"
      contains: "config.yaml"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/lifecycle-config.md"
      via: "Read directive"
      pattern: "references/lifecycle-config\\.md"
    - from: "skill/references/stage-transitions.md"
      to: ".lifecycle/config.yaml"
      via: "mode resolution from config layers"
      pattern: "config\\.yaml"
---

<objective>
Integrate the configuration system into SKILL.md and stage-transitions.md so that Claude actually uses config resolution during lifecycle operations.

Purpose: Plan 01 created the reference and template. This plan wires them into the existing skill so config is discoverable (SKILL.md) and active (stage-transitions reads mode from config layers). Also ensures CONF-03 is wired -- settings-changelog rules updated to cover lifecycle config.
Output: Updated `skill/SKILL.md` and `skill/references/stage-transitions.md`
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-configuration/11-RESEARCH.md
@.planning/phases/11-configuration/11-01-SUMMARY.md

@skill/SKILL.md
@skill/references/stage-transitions.md
@skill/references/lifecycle-config.md
@skill/templates/settings-changelog.md

<interfaces>
<!-- From Plan 01 outputs that this plan consumes -->

From skill/references/lifecycle-config.md (created in Plan 01):
- Resolution Algorithm: env var > config.yaml > defaults
- Operations: lifecycle-config get|set|list
- Settings: mode, skip_stages, proactive, auto_skip, verification_strictness
- Env var prefix: LIFECYCLE_

From skill/templates/config.yaml (created in Plan 01):
- 5 settings with defaults: mode=feature, skip_stages=[], proactive=true, auto_skip=true, verification_strictness=standard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Configuration section and lifecycle-config phase transition detection to SKILL.md</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md
    - skill/references/lifecycle-config.md
    - .planning/phases/11-configuration/11-RESEARCH.md
  </read_first>
  <action>
Add two blocks to SKILL.md. Target: ~15-20 lines added, keeping total under 360 lines.

**Block 1: Configuration section (~10 lines)**

Insert AFTER the "## Execution Modes" section (around line 278) and BEFORE "## Skill Orchestration" section. Add:

```markdown
## Configuration

Settings are managed through `.lifecycle/config.yaml` with 3-layer resolution:
1. **Environment variable:** `LIFECYCLE_{KEY}` (e.g., `LIFECYCLE_MODE=hotfix`)
2. **Config file:** `.lifecycle/config.yaml`
3. **Built-in defaults:** mode=feature, skip_stages=[], proactive=true, auto_skip=true, verification_strictness=standard

Config file is created from template on first `set` operation. System works with defaults when no config file exists.

Read: `$CLAUDE_SKILL_DIR/references/lifecycle-config.md`
```

**Block 2: Phase Transition Detection entries (~4 lines)**

In the existing "## Phase Transition Detection" table (around line 307-317), add these rows BEFORE the last row:

```
| "lifecycle-config get {key}" | Config query | Resolve and display setting value |
| "lifecycle-config set {key} {value}" | Config change | Write config, log to settings-changelog |
| "lifecycle-config list" / "config" / "settings" | Config overview | Display all resolved settings with sources |
```

**Important constraints:**
- Do NOT exceed 360 lines total (currently 339, adding ~15-20)
- Do NOT duplicate content from lifecycle-config.md reference
- Keep the Configuration section compact -- it's an entry point, not the full reference
- Preserve the existing architecture audit HTML comment at the bottom of the file
  </action>
  <verify>
    <automated>grep -c "lifecycle-config" skill/SKILL.md | xargs -I{} test {} -ge 3 && wc -l < skill/SKILL.md | xargs -I{} test {} -le 370 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Configuration" skill/SKILL.md returns match
    - grep "LIFECYCLE_" skill/SKILL.md returns match (env var mention)
    - grep "references/lifecycle-config.md" skill/SKILL.md returns match (Read directive)
    - grep "lifecycle-config get" skill/SKILL.md returns match (phase transition detection)
    - grep "lifecycle-config set" skill/SKILL.md returns match (phase transition detection)
    - grep "lifecycle-config list" skill/SKILL.md returns match (phase transition detection)
    - wc -l skill/SKILL.md returns <= 370 (under 500 budget, near estimate)
    - grep "config.yaml" skill/SKILL.md returns match
    - Existing "Architecture Audit" comment at end of file is preserved
  </acceptance_criteria>
  <done>SKILL.md has a Configuration section with Read directive to lifecycle-config.md, phase transition detection entries for get/set/list, and stays under 370 lines total</done>
</task>

<task type="auto">
  <name>Task 2: Update stage-transitions.md to resolve mode from config layers and update settings-changelog rules</name>
  <files>skill/references/stage-transitions.md</files>
  <read_first>
    - skill/references/stage-transitions.md
    - skill/references/lifecycle-config.md
    - skill/templates/settings-changelog.md
    - skill/templates/state.json
    - .planning/phases/11-configuration/11-RESEARCH.md
  </read_first>
  <action>
Update `skill/references/stage-transitions.md` in two places:

**Change 1: Config-aware mode resolution in Transition Procedure**

In the "## Transition Procedure" section, update Step 1 (currently: "Read state.json -- get current stage number and status"). Replace or augment it to include config resolution:

Change Step 1 from:
```
1. **Read state.json** -- get current stage number and status
```
To:
```
1. **Read state and config** -- get current stage, status from state.json. Resolve mode using config layers:
   a. Check env: `echo "${LIFECYCLE_MODE:-}"` -- if set, use this
   b. If empty: read `.lifecycle/config.yaml` `mode:` value
   c. If not found: use state.json `current.mode` (backward compat default)
   Update state.json `current.mode` if config layer value differs.
```

**Change 2: Config-aware mode in Auto-Skip Procedure**

In the "## Auto-Skip Procedure" section, update Step 2. Currently says:
```
2. Read current mode from state.json (`current.mode`)
```
Change to:
```
2. Resolve current mode using config layers (env > .lifecycle/config.yaml > state.json). See lifecycle-config reference for full resolution algorithm.
```

**Change 3: Add note about skip_stages config**

In the "## Auto-Skip Procedure" section, after Step 3 ("Look up skippable stages for current mode"), add:

```
3b. Check `.lifecycle/config.yaml` `skip_stages:` list. Union with mode skippable stages (additive -- config skip_stages supplements mode defaults, never removes required stages). Also check env: `LIFECYCLE_SKIP_STAGES` (comma-separated integers).
```

**Change 4: Add config reference pointer**

At the end of "## Mode-Based Stage Skipping" section, add:

```
Configuration reference: `$CLAUDE_SKILL_DIR/references/lifecycle-config.md`
Settings changes are logged to `.lifecycle/settings-changelog.md` (see SKILL.md Configuration section).
```

**Do NOT change:**
- Gate conditions (those stay as-is)
- Status values
- Backward transitions logic
- Artifact output format
- History entry format
  </action>
  <verify>
    <automated>grep -c "config.yaml\|LIFECYCLE_MODE\|config layers\|lifecycle-config" skill/references/stage-transitions.md | xargs -I{} test {} -ge 4 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "config layers" skill/references/stage-transitions.md returns match (new resolution language)
    - grep "LIFECYCLE_MODE" skill/references/stage-transitions.md returns match (env var check)
    - grep "config.yaml" skill/references/stage-transitions.md returns match (config file layer)
    - grep "skip_stages" skill/references/stage-transitions.md returns match (config skip_stages integration)
    - grep "lifecycle-config" skill/references/stage-transitions.md returns match (reference pointer)
    - grep "settings-changelog" skill/references/stage-transitions.md returns match (CONF-03 integration)
    - Original gate conditions still present: grep "artifacts.1_plan" returns match
    - Original status values still present: grep "not_started" returns match
  </acceptance_criteria>
  <done>stage-transitions.md resolves mode from 3-layer config (env > config.yaml > state.json fallback), integrates skip_stages config, and references lifecycle-config.md. Original gate/status/history logic preserved unchanged.</done>
</task>

</tasks>

<verification>
1. `skill/SKILL.md` has Configuration section with Read directive -- `grep "## Configuration" skill/SKILL.md`
2. `skill/SKILL.md` under 370 lines -- `wc -l skill/SKILL.md`
3. `skill/references/stage-transitions.md` uses config layers for mode -- `grep "config layers" skill/references/stage-transitions.md`
4. Phase transition detection includes lifecycle-config commands -- `grep -c "lifecycle-config" skill/SKILL.md` returns >= 3
5. CONF-03 wiring: settings-changelog referenced from stage-transitions -- `grep "settings-changelog" skill/references/stage-transitions.md`
</verification>

<success_criteria>
- SKILL.md has Configuration section (~10 lines) + 3 phase transition detection entries
- SKILL.md total lines <= 370 (well within 500 budget)
- stage-transitions.md resolves mode from config layers instead of only state.json
- skip_stages config is integrated into auto-skip procedure
- All CONF-01/02/03 requirements are now wired end-to-end through the skill
</success_criteria>

<output>
After completion, create `.planning/phases/11-configuration/11-02-SUMMARY.md`
</output>
