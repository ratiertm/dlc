---
phase: 11-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/templates/config.yaml
  - skill/references/lifecycle-config.md
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "Config template defines all 5 settings (mode, skip_stages, proactive, auto_skip, verification_strictness) with defaults and comments"
    - "Reference document describes 3-layer resolution algorithm (env > config.yaml > defaults)"
    - "Reference document includes get/set/list procedures with mandatory changelog logging on every set"
  artifacts:
    - path: "skill/templates/config.yaml"
      provides: "Default config template with all 5 settings"
      contains: "mode: feature"
    - path: "skill/references/lifecycle-config.md"
      provides: "Config operations reference for Claude instruction-following"
      min_lines: 80
  key_links:
    - from: "skill/references/lifecycle-config.md"
      to: "skill/templates/config.yaml"
      via: "template copy on first set"
      pattern: "templates/config\\.yaml"
    - from: "skill/references/lifecycle-config.md"
      to: ".lifecycle/settings-changelog.md"
      via: "mandatory append on every set operation"
      pattern: "settings-changelog"
---

<objective>
Create the configuration system foundation: a default config template and a comprehensive reference document that instructs Claude how to handle lifecycle-config get/set/list operations with 3-layer resolution and automatic change tracking.

Purpose: This is the core of Phase 11 -- the template and reference are the "implementation" for a Claude Code skill. All downstream integration (SKILL.md, stage-transitions) depends on these files existing first.
Output: `skill/templates/config.yaml` and `skill/references/lifecycle-config.md`
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-configuration/11-RESEARCH.md

@skill/templates/settings-changelog.md
@skill/templates/state.json
@skill/references/stage-transitions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.yaml template with all 5 settings</name>
  <files>skill/templates/config.yaml</files>
  <read_first>
    - skill/templates/state.json
    - skill/templates/settings-changelog.md
    - .planning/phases/11-configuration/11-RESEARCH.md
  </read_first>
  <action>
Create `skill/templates/config.yaml` with the following exact content. This is the default configuration template that gets copied to `.lifecycle/config.yaml` on first use.

```yaml
# Dev Lifecycle Configuration
# Manage with: lifecycle-config get|set|list
# Env overrides: LIFECYCLE_<KEY> (e.g., LIFECYCLE_MODE=hotfix)

# Execution mode: hotfix | feature | release | milestone
mode: feature

# Stages to always skip (list of stage numbers)
# Overrides mode defaults when non-empty
skip_stages: []

# Suggest companion skills at stage transitions
proactive: true

# Auto-skip skippable stages without prompting
auto_skip: true

# Verification strictness: strict | standard | relaxed
# strict: all spec steps must pass, no overrides
# standard: all must pass, override with justification
# relaxed: warnings only, no blocking
verification_strictness: standard
```

All 5 settings must be present with their defaults: mode=feature, skip_stages=[], proactive=true, auto_skip=true, verification_strictness=standard.
  </action>
  <verify>
    <automated>grep -c "mode:\|skip_stages:\|proactive:\|auto_skip:\|verification_strictness:" skill/templates/config.yaml | grep -q "5" && echo "PASS: all 5 settings present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "mode: feature" skill/templates/config.yaml returns match
    - grep "skip_stages: \[\]" skill/templates/config.yaml returns match
    - grep "proactive: true" skill/templates/config.yaml returns match
    - grep "auto_skip: true" skill/templates/config.yaml returns match
    - grep "verification_strictness: standard" skill/templates/config.yaml returns match
    - grep "LIFECYCLE_" skill/templates/config.yaml returns match (env override comment)
    - File has comment header explaining usage
  </acceptance_criteria>
  <done>Template file exists at skill/templates/config.yaml with all 5 settings, defaults, inline comments, and env override instructions</done>
</task>

<task type="auto">
  <name>Task 2: Create lifecycle-config.md reference with resolution algorithm, get/set/list procedures, and changelog integration</name>
  <files>skill/references/lifecycle-config.md</files>
  <read_first>
    - skill/templates/config.yaml
    - skill/templates/settings-changelog.md
    - skill/references/stage-transitions.md
    - skill/templates/state.json
    - .planning/phases/11-configuration/11-RESEARCH.md
  </read_first>
  <action>
Create `skill/references/lifecycle-config.md` (~100-120 lines) with these sections:

**1. Header** -- title and purpose (config operations reference for dev-lifecycle)

**2. Settings Table** -- all 5 settings with columns: Setting, Type, Default, Valid Values, Description. Exact values:
- mode: string, feature, hotfix|feature|release|milestone, "Execution mode determining which stages are required"
- skip_stages: list[int], [], "Stage numbers 1-9", "Explicit stage skip overrides (additive with mode defaults)"
- proactive: boolean, true, true|false, "Suggest companion skills at stage transitions"
- auto_skip: boolean, true, true|false, "Auto-skip skippable stages without prompting"
- verification_strictness: string, standard, strict|standard|relaxed, "How strictly to enforce spec verification"

**3. Resolution Algorithm (CONF-02)** -- 3-layer precedence, first match wins:
1. Environment variable: `LIFECYCLE_{SETTING_UPPER}` (e.g., LIFECYCLE_MODE). Check via `echo "${LIFECYCLE_MODE:-}"`. If non-empty, use this value.
2. Config file: `.lifecycle/config.yaml`. Read the file, find the key. If file doesn't exist, skip to layer 3.
3. Built-in defaults: hardcoded defaults from settings table above.

Include instruction text: "To resolve setting `{key}`: 1) Run `echo "${LIFECYCLE_{KEY_UPPER}:-}"` -- if non-empty, use this value. 2) If empty, read `.lifecycle/config.yaml`, find `{key}:` line, extract value. 3) If not found or file missing, use default from Settings Table."

**4. Operations (CONF-01)**

**4a. `lifecycle-config get {key}`:**
1. Validate key is one of the 5 known settings
2. Resolve value using Resolution Algorithm
3. Display: `{key}: {value} (source: {environment|config.yaml|default})`

**4b. `lifecycle-config set {key} {value}`:**
1. Validate key is known, value is valid for that key's type/constraints
2. Read current resolved value (for changelog old->new)
3. If `.lifecycle/config.yaml` doesn't exist, copy from `$CLAUDE_SKILL_DIR/templates/config.yaml`
4. Write new value to `.lifecycle/config.yaml`
5. Ask user for reason (one-liner)
6. **Mandatory changelog:** Append to `.lifecycle/settings-changelog.md`:
   `| {date} | .lifecycle/config.yaml | {key}: {old} -> {new} | {reason} |`
   If settings-changelog.md doesn't exist, create from template first.
7. If key is `mode`: also update `state.json` `current.mode` for backward compatibility
8. Confirm: `{key} set to {value} (was: {old})`

Note: Step 6 is MANDATORY, not optional. This fulfills CONF-03.

**4c. `lifecycle-config list`:**
1. For each of the 5 settings, resolve value using Resolution Algorithm
2. Display table format:
   ```
   Dev Lifecycle Configuration:

     mode:                      feature     (default)
     skip_stages:               []          (config.yaml)
     proactive:                 true        (default)
     auto_skip:                 true        (default)
     verification_strictness:   standard    (default)

   Config file: .lifecycle/config.yaml (exists|not found)
   Env overrides: {list or "none detected"}
   ```

**5. Validation Rules:**
- mode: must be one of hotfix|feature|release|milestone
- skip_stages: list of integers 1-9
- proactive: must be true or false
- auto_skip: must be true or false
- verification_strictness: must be one of strict|standard|relaxed
- Unknown keys: reject on set, ignore on read

**6. Edge Cases:**
- Config file doesn't exist: resolution falls through to defaults. `list` shows all defaults. `get` returns default. First `set` copies template then writes.
- skip_stages interaction with mode: Union (additive). skip_stages adds to mode's skippable stages, never removes required stages.
- auto_skip scope: applies to all skippable stages (both mode-based and explicit skip_stages)
- Env var for skip_stages: comma-separated integers, e.g., LIFECYCLE_SKIP_STAGES="5,6,9"
- Env var for proactive/auto_skip: string "true" or "false"

**7. Integration Notes:**
- Config is source of truth for mode; state.json reflects it (updated on set)
- Session start: resolve mode from config, write to state.json for backward compat
- settings-changelog.md now also tracks lifecycle config changes (same format as project config changes)
  </action>
  <verify>
    <automated>grep -c "Resolution Algorithm\|lifecycle-config get\|lifecycle-config set\|lifecycle-config list\|Settings Table\|Validation" skill/references/lifecycle-config.md | xargs -I{} test {} -ge 5 && echo "PASS: all sections present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Resolution Algorithm" skill/references/lifecycle-config.md returns match
    - grep "LIFECYCLE_" skill/references/lifecycle-config.md returns match (env var prefix)
    - grep "lifecycle-config get" skill/references/lifecycle-config.md returns match
    - grep "lifecycle-config set" skill/references/lifecycle-config.md returns match
    - grep "lifecycle-config list" skill/references/lifecycle-config.md returns match
    - grep "settings-changelog" skill/references/lifecycle-config.md returns match (CONF-03 link)
    - grep "mode.*feature" skill/references/lifecycle-config.md returns match (default)
    - grep "Mandatory" skill/references/lifecycle-config.md returns match (changelog is mandatory)
    - File has at least 80 lines: wc -l returns >= 80
  </acceptance_criteria>
  <done>Reference file exists at skill/references/lifecycle-config.md with complete resolution algorithm (3 layers), all 3 operations (get/set/list), validation rules, and mandatory changelog integration</done>
</task>

</tasks>

<verification>
1. `skill/templates/config.yaml` exists with exactly 5 settings and correct defaults
2. `skill/references/lifecycle-config.md` exists with >= 80 lines covering resolution, operations, validation
3. Both files reference each other appropriately (template is referenced from reference doc)
4. CONF-03 is addressed: changelog logging is documented as mandatory in set procedure
</verification>

<success_criteria>
- Config template has all 5 settings with correct defaults and types
- Reference document covers 3-layer resolution (CONF-02), get/set/list operations (CONF-01), and mandatory changelog logging (CONF-03)
- No SKILL.md changes yet (that's Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/11-configuration/11-01-SUMMARY.md`
</output>
