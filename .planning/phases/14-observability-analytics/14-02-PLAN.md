---
phase: 14-observability-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - skill/SKILL.md
  - skill/references/stage-transitions.md
  - skill/references/plan-stage.md
autonomous: true
requirements:
  - OBSV-01
  - OBSV-02
  - OBSV-03
  - OBSV-04
  - OBSV-05

must_haves:
  truths:
    - "SKILL.md has an Observability section with Read directive to observability.md"
    - "Session Start creates session context file as step 6"
    - "Stage transition procedure appends JSONL at step 5b"
    - "Backward transitions append rework event JSONL"
    - "PLAN completion creates spec baseline before DO transition"
    - "Phase Transition Detection table has analytics/spec-diff/session triggers"
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Observability section, session start step 6, analytics triggers"
      contains: "## Observability"
    - path: "skill/references/stage-transitions.md"
      provides: "JSONL append at step 5b, rework JSONL at backward transitions"
      contains: "5b."
    - path: "skill/references/plan-stage.md"
      provides: "Spec baseline snapshot at PLAN completion"
      contains: "spec-baselines"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/references/observability.md"
      via: "Read directive"
      pattern: "references/observability\\.md"
    - from: "skill/references/stage-transitions.md"
      to: ".lifecycle/analytics/stage-transitions.jsonl"
      via: "Step 5b JSONL append"
      pattern: "stage-transitions\\.jsonl"
    - from: "skill/references/stage-transitions.md"
      to: ".lifecycle/analytics/rework-events.jsonl"
      via: "Backward transition JSONL append"
      pattern: "rework-events\\.jsonl"
    - from: "skill/references/plan-stage.md"
      to: ".lifecycle/analytics/spec-baselines/"
      via: "Baseline copy at PLAN completion"
      pattern: "spec-baselines"
---

<objective>
Wire observability into the existing dev-lifecycle skill files -- SKILL.md, stage-transitions.md, and plan-stage.md.

Purpose: Plan 01 created the reference file with all logic. This plan adds the minimal integration points that trigger observability features: session context creation at Session Start, JSONL appends at stage transitions, spec baseline at PLAN completion, and analytics trigger keywords in Phase Transition Detection.

Output: Modified SKILL.md (Observability section + Session Start step 6 + analytics triggers), stage-transitions.md (step 5b + rework JSONL), plan-stage.md (spec baseline step).
</objective>

<execution_context>
@/Users/leeeunmi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leeeunmi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-observability-analytics/14-RESEARCH.md
@.planning/phases/14-observability-analytics/14-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 output: the reference file this plan wires into -->

skill/references/observability.md sections:
- ## Session Context Files -- creation at Session Start step 6, update at transitions
- ## Stage Transition Analytics -- JSONL append as step 5b in Transition Procedure
- ## Rework Event Tracking -- JSONL append on backward transitions
- ## Spec Baseline & Diff -- baseline copy at PLAN completion
- ## Time-Per-Stage Metrics -- on-demand report from JSONL data
- ## Analytics Commands -- trigger keyword table

<!-- Current SKILL.md structure (365 lines, budget 500) -->

SKILL.md section order:
1. Frontmatter + header (lines 1-16)
2. The 9-Stage Pipeline (lines 17-33)
3. Session Start (lines 35-61) -- steps 1-5
4. Stage Overview (lines 63-213) -- stages 1-9
5. State Management (lines 214-233)
6. Memory & Decision Trail (lines 235-262)
7. Stage Transitions (lines 264-273)
8. Execution Modes (lines 275-284)
9. Configuration (lines 286-296)
10. Skill Orchestration (lines 298-311)
11. Project Type Detection (lines 313-319)
12. Phase Transition Detection (lines 321-336)
13. Edge Cases (lines 337-343)
14. Output Language (lines 344-346)
15. Upgrade (lines 348-353)
16. Architecture Audit comment (lines 355-365)

<!-- stage-transitions.md Transition Procedure (lines 79-123) -->

Current Transition Procedure steps:
1. Read state and config
2. Check gate condition
3. If gate passes: update state.json
3.5. Check auto-skip
4. If gate fails: set blocked
5. Write history entry to .lifecycle/history/
6. Update Living State

<!-- plan-stage.md completion area -->

Plan stage ends with: Step 4 Agreement Gate, then transition to DO.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add JSONL append and rework tracking to stage-transitions.md</name>
  <files>skill/references/stage-transitions.md</files>
  <read_first>
    - skill/references/stage-transitions.md
    - skill/references/observability.md
  </read_first>
  <action>
Modify `skill/references/stage-transitions.md` with two additions:

**Addition 1: Step 5b in Transition Procedure (after Step 5, before Step 6)**

Insert after the Step 5 "Write history entry" block (after the closing ``` of the JSON example, before Step 6):

```markdown
5b. **Append analytics JSONL** (Read: `$CLAUDE_SKILL_DIR/references/observability.md` Section "Stage Transition Analytics"):
   - Create `.lifecycle/analytics/` directory if not exists
   - Compute `duration_in_stage_seconds`: current timestamp minus `progress.current_stage_started_at` (integer seconds)
   - Determine `direction`: "forward" if to_stage > from_stage, "backward" if to_stage < from_stage, "skip" if auto-skipped
   - Append one JSON line to `.lifecycle/analytics/stage-transitions.jsonl`:
     ```json
     {"timestamp":"{ISO}","feature":"{name}","from_stage":{N},"from_name":"{NAME}","to_stage":{M},"to_name":"{NAME}","direction":"{forward|backward|skip}","mode":"{mode}","gate_result":"{result}","duration_in_stage_seconds":{seconds}}
     ```
   - Non-blocking: if write fails, warn and continue transition
```

**Addition 2: Rework JSONL in Backward Transitions section**

Find the "## Backward Transitions" section. After the existing bullet points (Do NOT require gate checks, Reset target stage, Remove from stages_completed, Record backward transition), add:

```markdown
- Append rework event to `.lifecycle/analytics/rework-events.jsonl` (Read: `$CLAUDE_SKILL_DIR/references/observability.md` Section "Rework Event Tracking"):
  ```json
  {"timestamp":"{ISO}","feature":"{name}","from_stage":{N},"from_name":"{NAME}","to_stage":{M},"to_name":"{NAME}","reason":"{reason}","impact":"{what needs redo}","rework_count":{count}}
  ```
  Where `rework_count` = number of existing rework entries for this feature + 1
```

Do NOT modify any other sections. Keep all existing content intact.
  </action>
  <verify>
    <automated>grep -n "5b\.\|rework-events\.jsonl\|stage-transitions\.jsonl\|duration_in_stage_seconds" skill/references/stage-transitions.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "5b\." skill/references/stage-transitions.md
    - grep -q "stage-transitions.jsonl" skill/references/stage-transitions.md
    - grep -q "rework-events.jsonl" skill/references/stage-transitions.md
    - grep -q "duration_in_stage_seconds" skill/references/stage-transitions.md
    - grep -q "Non-blocking" skill/references/stage-transitions.md
    - grep -q "rework_count" skill/references/stage-transitions.md
    - grep -q "references/observability.md" skill/references/stage-transitions.md
  </acceptance_criteria>
  <done>
    - Transition Procedure has step 5b appending JSONL with all required fields (OBSV-02, OBSV-05)
    - Backward Transitions section has rework-events.jsonl append with reason/impact/rework_count (OBSV-03)
    - Both additions include Read directive to observability.md for full logic
    - Non-blocking behavior specified (analytics failure does not block transition)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add spec baseline to plan-stage.md and wire Observability into SKILL.md</name>
  <files>skill/references/plan-stage.md, skill/SKILL.md</files>
  <read_first>
    - skill/references/plan-stage.md
    - skill/SKILL.md
    - skill/references/observability.md
  </read_first>
  <action>
**Part A: Add spec baseline snapshot to plan-stage.md (OBSV-04)**

Find the Agreement Gate section (Step 4) in plan-stage.md. After the agreement gate passes and before any transition-to-DO logic, add a new sub-step:

```markdown
**4b. Spec Baseline Snapshot** (Read: `$CLAUDE_SKILL_DIR/references/observability.md` Section "Spec Baseline & Diff"):
   - Create `.lifecycle/analytics/spec-baselines/` directory if not exists
   - If `.lifecycle/analytics/spec-baselines/{feature}.baseline.md` does NOT exist:
     - Copy `.lifecycle/features/{feature}/spec.md` to `.lifecycle/analytics/spec-baselines/{feature}.baseline.md`
   - If baseline already exists: skip (preserve original baseline)
```

**Part B: Wire Observability into SKILL.md (3 changes, budget: ~12 new lines)**

SKILL.md is at 365 lines (budget 500). Target: stay under 380 after changes.

**Change 1: Add step 6 to Session Start (after step 5 "Check gates")**

After line `5. **Check gates:** Verify manifest.json for current stage requirements`, add:

```markdown
6. **Session context:** Create session file in `.lifecycle/sessions/` (if `.lifecycle/` exists and feature is active)
   - Read: `$CLAUDE_SKILL_DIR/references/observability.md` Section "Session Context Files"
```

**Change 2: Add Observability section (between Upgrade and Architecture Audit comment)**

Insert before the `<!-- Architecture Audit` comment block:

```markdown
## Observability

Session context files, stage transition analytics, rework tracking, spec diffs, and time-per-stage metrics. Data in `.lifecycle/sessions/` and `.lifecycle/analytics/`. All analytics are non-blocking (never delay stage transitions).

Read: `$CLAUDE_SKILL_DIR/references/observability.md`
```

**Change 3: Add analytics triggers to Phase Transition Detection table**

Add these rows to the existing table in the Phase Transition Detection section (after the `/gsd:complete-milestone` row):

```markdown
| "analytics" / "metrics" / "time per stage" | Analytics query | Read observability.md, generate time-per-stage report |
| "spec diff" / "what changed from spec" | Spec comparison | Read observability.md, generate spec baseline diff |
| "session history" / "sessions" | Session review | Read observability.md, list/summarize session files |
| "rework report" | Rework analysis | Read observability.md, summarize rework events |
```

**Change 4: Update Architecture Audit comment**

Update the ARCH-01 line count in the HTML comment at the end of SKILL.md to reflect the new line count (should be ~377 lines). Update audit date to 2026-03-25.

Do NOT modify any other sections. All existing content must remain intact.
  </action>
  <verify>
    <automated>wc -l skill/SKILL.md && grep -n "Observability\|observability\.md\|Session context\|analytics\|spec diff\|rework report" skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "## Observability" skill/SKILL.md
    - grep -q "references/observability.md" skill/SKILL.md
    - grep -q "Session context" skill/SKILL.md
    - grep -q "spec diff" skill/SKILL.md
    - grep -q "rework report" skill/SKILL.md
    - grep -q "analytics" skill/SKILL.md
    - test $(wc -l < skill/SKILL.md) -le 385
    - grep -q "spec-baselines" skill/references/plan-stage.md
    - grep -q "4b" skill/references/plan-stage.md
    - grep -q "baseline.md" skill/references/plan-stage.md
    - grep -q "references/observability.md" skill/references/plan-stage.md
  </acceptance_criteria>
  <done>
    - SKILL.md has Observability section with Read directive (entry point for all observability features)
    - Session Start step 6 creates session context file with Read directive (OBSV-01)
    - Phase Transition Detection table has 4 new analytics trigger rows (OBSV-02/03/04/05)
    - SKILL.md line count stays under 385 (within 500 budget)
    - plan-stage.md step 4b creates spec baseline before DO transition (OBSV-04)
    - Architecture Audit comment updated with new line count
  </done>
</task>

</tasks>

<verification>
- SKILL.md line count under 385 (verify: `wc -l skill/SKILL.md`)
- All 3 modified files reference observability.md via Read directives
- Session Start has 6 steps (was 5)
- Transition Procedure has step 5b
- Plan stage has step 4b
- Phase Transition Detection has analytics/spec-diff/sessions/rework triggers
- No existing functionality broken (all original content preserved)
</verification>

<success_criteria>
- Observability is wired into the 3 key integration points: Session Start (OBSV-01), Stage Transitions (OBSV-02/03/05), PLAN completion (OBSV-04)
- SKILL.md stays slim via Read directives (not inlining logic)
- Analytics triggers enable on-demand report generation
- SKILL.md under 385 lines total
</success_criteria>

<output>
After completion, create `.planning/phases/14-observability-analytics/14-02-SUMMARY.md`
</output>
